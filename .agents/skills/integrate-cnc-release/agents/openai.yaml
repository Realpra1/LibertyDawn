interface:
  display_name: "Integrate CNC Release"
  short_description: "Merge and test a coordinated CNC release"
  default_prompt: "Use $integrate-cnc-release to combine reviewed CNC task branches and manage integrated testing."
policy:
  allow_implicit_invocation: false
