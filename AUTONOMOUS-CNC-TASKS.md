# Autonomous CNC Task Sheet

## Workflow

- Supported game/mod: LibertyDawn Command & Conquer (`cnc`) only. Do not build, test, package, or modify Red Alert, Dune 2000, or Tiberian Sun unless shared engine compilation makes it unavoidable.
- Use the coordinated workflow in `.agents/skills/coordinate-cnc-development/`. Only a Task Maker role edits this sheet; the coordinator, speccers, workers, reviewers, and integrator receive isolated role-specific files instead.
- Start each development round from one freshly fetched and recorded checkpoint, normally `origin/bleed` or the user-designated release base. Base every task branch in that round on the exact same commit and open one individual PR per task.
- After individual testing and review, locally merge the reviewed task heads into one stable release branch, advance its tested candidate head through integrated repair rounds, and publish its single final release PR to `bleed`. Do not push directly to `bleed` or invoke GitHub merge on the individual task PRs.
- CNC-26C is permanently pinned as the final task. Complete every other pending task first, and insert all future tasks before CNC-26C regardless of their identifier or when they are added. CNC-26C also remains ineligible until its required user-design questions are answered.
- The aged insane PR may be inspected for ideas and provenance, but its code is not an authoritative base. Preserve LibertyDawn balance, resource growth, harvester logic, and later improvements.
- A worker may propose `complete - testing` or `first iteration - testing` after its isolated implementation, game evidence, report, PR, review response, and green required checks. Final `complete` or `first iteration` requires the integrated release result. Record cycle counts and evidence.

Status values: `draft`, `pending`, `in progress`, `complete - testing`, `first iteration - testing`, `complete`, `first iteration`, `to be resumed`.

| ID | Status | Task |
|---|---|---|
| CNC-87 | in progress — claimed for coordinated round 20260807-bug-polish-02 | **Repair current Codex CLI launching, nested-role configuration, and large-build resource enforcement for coordinated external roles.** What happened: `.agents/skills/coordinate-cnc-development/scripts/launch_role.py` invokes the installed current `codex exec` with the obsolete `-a never` option, which the CLI rejects before an external role session starts. Separately, a worker launched a fresh Terra match Commenter with `model_reasoning_effort=max` although the coordinator contract requires Terra medium. During coordinated RC1 construction, a worker reserved `large-build-1.lock` through `with_resource_slots.py --resource large-build --capacity 1` while the Integrator simultaneously acquired `large-build.lock` with `flock`, allowing both large build/test suites to run. These failures are wrong because, after native agent slots are exhausted, a coordinated round depends on external sessions and its role split is intended to reduce cost and context use; a rejected launcher prevents one full five-worker round from launching reliably, maximum-reasoning analysis roles defeat the design and make a full round unnecessarily expensive, and separate lock names/protocols silently bypass the configured capacity-one guard, causing CPU/RAM contention and making coordinator resource status untrustworthy. Replace the obsolete approval-policy argument with the supported current CLI mechanism for approval policy `never`, retaining the launcher’s pinned sandbox, model, reasoning, output, and supervision behavior. Make worker-spawned Commenter and Policy Reviewer role/model/reasoning selection reliable and enforced rather than relying on a worker to reconstruct a command: ordinary Commenters and Policy Reviewers must use exactly `gpt-5.6-terra` at medium reasoning, while documented higher-level policy-role exceptions remain intact. Define and enforce one canonical slot-aware lock protocol and namespace for every worker, reviewer, and integrator large build: capacity one must serialize all entry paths, while independent game slots remain available. Add fast local smoke/regression validation that checks the constructed `codex exec` invocation against the installed CLI’s accepted options without consuming an agent run, verifies nested-role configuration enforcement, and launches two independent large-build reservation probes through the actual worker and integrator entry paths. That regression must prove the second probe cannot enter until the first releases, then prove stale or failed holders release safely; it must fail clearly if process trees overlap, both `large-build.lock` and `large-build-1.lock` exist, or either path can acquire while the other is held. The expected result is that external launches begin successfully under the current CLI, fresh isolated analysis roles use the coordinator-prescribed configuration, and simultaneous large builds queue behind the same capacity-one reservation. Do not merely document the setting, weaken sandbox/path-envelope validation, prevent workers from running fresh isolated analysis roles, weaken approval, sandbox, model, reasoning, output, or monitoring settings merely to make the command parse, or weaken resource isolation to avoid serialization. |
| CNC-19 | complete (11 cycles; PR #38 green) | **OpenRA rebase/backport.** In its own focused PR, compare current LibertyDawn with current OpenRA and the aged insane PR. Port compatible engine improvements without changing LibertyDawn balance or gameplay changes. Prioritize the upstream fix that prevents repeated failed unit routes from consuming excessive resources/freezing the game. Prove LibertyDawn resource growth, harvester behavior, and other local improvements remain intact. |
| CNC-20 | complete (12 cycles; PR #39 green) | **Refactor.** Inspect current code and the aged insane PR for concrete modularity/maintainability opportunities. Refactor bounded high-value areas without changing behavior; split oversized responsibilities and leave unrelated opportunities in `DEFERRED_WORK.md`. |
| CNC-21 | complete (12 cycles; PR #40 green) | **Transport recovery.** After persistent route failure, transport eligible units by helicopter, with at most ten transport helicopters built. Prefer safe routing using the air-AI threat model; otherwise take the least dangerous route even when long. Station idle carriers in the base or another safe location and repair damaged carriers. |
| CNC-22 | complete (8 cycles; PR #41 green) | **Supply-truck stall recovery.** Send AI supply trucks into its own eligible buildings using normal player orders. Recover at least every ten seconds when the destination is destroyed, inaccessible, or blocked by walls, fixing the stall observed in PR #37. |
| CNC-23 | complete (11 cycles; PR #42 green) | **Allied recovery help.** Build and send supply trucks to an allied AI that is out of money and has no harvesters or refineries. Limit aid to one truck per five minutes per available war factory/airfield, stop when the ally regains an MCV, harvester, or refinery, and give up when it has no production building, MCV, or moving unit. |
| CNC-24 | complete (14 cycles; PR #43 green) | **APC rush.** As an optional early-game strategy selected in roughly 50% of starts, load infantry into APCs, drive near the enemy base, then unload for attack, capture, or C4. Do not attack/unload early unless seen and shot. Later, use APCs occasionally for long travel across hazards or surprise attacks. If APC technology is unavailable, allow a transport helicopter. Normal scaling remains the other opening strategy. |
| CNC-24.5 | complete (20 cycles; PR #45 green) | **MAX automated-test game speed.** Add a local/debug skirmish speed named `MAX` that advances the fixed-step deterministic simulation as fast as the machine can process it by removing ordinary wall-clock frame pacing, without changing simulation ticks, game rules, AI intervals, RNG, replay results, or normal speed modes. Make it selectable by the autonomous/debug launch path and reject it for network games. Keep input/window event processing and bounded yielding responsive enough to stop a test, and avoid log/render overhead defeating the mode where practical. Compare elapsed wall time for the same deterministic match at Fastest and MAX, verify the same final checksum/replay outcome, confirm real AIs load and act normally, exercise save/load, and run a long high-unit-count match to detect starvation, runaway memory, hangs, or timing assumptions incorrectly tied to real time. The dedicated no-graphics automation path and graphical lobby presentation are follow-up CNC-24.6 rather than completion requirements here. |
| CNC-24.6 | complete (14 cycles; PR #46 green) | **Separate headless automated MAX from graphical lobby MAX.** Add an explicit no-graphics/headless MAX launch path for autonomous tests so simulation, AI, logging, replay/checksum evidence, automated saves, benchmark collection, and natural game-over exit work without initializing or rendering the game view where technically practical. Keep the local lobby's visible `MAX` game-speed option as a distinct graphical mode with normal graphics, window/input responsiveness, and enough rendering for a player to watch and control the match; do not let headless optimization disable graphics merely because a player selected MAX in the lobby. Both paths must preserve the same fixed deterministic simulation rules and remain unavailable to network games. Before using identical outcomes as a MAX acceptance gate, first launch two independent Fastest matches with the exact same commit, assets, map, explicit world seed, slots, factions, alliances, options, starting state, and no human orders. Compare periodic synchronization hashes/checksums, replays, AI orders where useful, and final outcome. If those Fastest controls reproduce, validate graphical and headless MAX against the same setup and require equivalent synchronized outcomes. If the Fastest controls diverge, investigate and document the existing nondeterminism, but do not declare MAX broken solely for failing an impossible deterministic comparison; instead use best-effort replay/OOS checks, invariants, behavior, and regression evidence. Never alter, replace, reseed during play, or otherwise damage the world RNG/game rules merely to force matching tests. Compare rendered versus headless MAX throughput, run a complete real-AI match in each mode, and test stop/exit, fatal-error visibility, save/load, minimized graphical windows, and high-unit-count behavior. First smoke cycle loaded all five real bots in a hidden window but stalled at world tick 0 because the pre-headless `renderBeforeNextTick` gate was never cleared; fix that gate before retesting. |
| CNC-25 | complete (22 resumed cycles; previous 28-cycle history retained; PR #44 green) | **Mammoth drop.** Add a repeatable mid/late-game strategy that loads roughly ten Mammoth tanks into transport helicopters and drops them in an undefended area or behind the enemy base. Generalize idle transports for slow units making long journeys where appropriate. Two manual `ArchipelagoTest` regressions are preserved. The correction routes every selected carrier concurrently to a distinct free pickup cell and waits for that carrier to be exactly positioned and landed before ordering its passenger aboard. Successful unload explicitly adopts all surviving passengers into one cohesive assault squad using the retained or re-evaluated target; safe aborts restore ordinary squad eligibility. The actual idle boundary was the lack of explicit handoff combined with the randomized ordinary assault threshold, not stale `activeUnits` state. Exact-ten, casualty, defended-abort, rescue-contention, ordinary-Fastest full-match, and final original-layout engine tests passed. Evidence: `AUTONOMOUS-CNC-LOGS/cnc25-regression-archipelagoTest-20260804-1144/`, `AUTONOMOUS-CNC-LOGS/cnc25-regression-archipelagoTest-20260804-1200-slow-load-orphaned-handoff/`, and `AUTONOMOUS-CNC-LOGS/cnc25-cycle17-*` through `cnc25-cycle22-*`. Preserve existing report and PR history. |
| CNC-26 | complete (12 cycles; PR #47 green) | **Technology switching.** Add configurable delayed technology switching for Iron Reaper only, defaulting to about two minutes after an enemy switch. Counter relationships are covert over economy, recon over covert, and economy over recon. Every AI must still use any technology it owns, including units/buildings captured from other branches. |
| CNC-26A | complete (11 cycles; PR #48 green) | **Iron Reaper all-technologies lobby mode.** When the lobby's all-technologies option is enabled (verify whether this is exposed as `Technology Level: Extra`), allow Iron Reaper alone to use the economy, recon, and covert technology branches simultaneously. All other AIs must retain their existing single-branch behavior. Test both the enabled and disabled lobby settings and confirm ordinary AIs are unchanged. |
| CNC-66 | complete (4 cycles) | **Repair the exhaustive OpenRA validator.** Diagnose why `utility.cmd cnc --check-yaml` can run for more than 20 minutes without diagnostics or completion. Fetch and inspect current official OpenRA history, commits, issues, and validator/lint implementations for upstream fixes or architectural improvements; backport only compatible engine/tooling changes without changing LibertyDawn gameplay or supported-mod scope. First distinguish a true infinite/redundant scan from legitimately slow maps/rules and identify the specific lint/pass/map that dominates runtime using bounded progress/timing instrumentation. Preserve deterministic validation results and failure coverage; do not merely skip maps, traits, or checks to manufacture speed. Add a timeout/fatal diagnostic that identifies the active validation stage if safe. Compare before/after wall time and CPU/memory on the same CNC rules/maps, require the validator to finish successfully with equivalent diagnostics, and run the normal strict build, unit/interface checks, and GitHub Linux/Windows checks. |
| CNC-66A | first iteration (21 cycles; PR #52 green; real-Mac verification pending) | **Fix the Empire Lars startup crash.** Reproduce the crash when opening the Empire Lars map with at least one human player and one Skynet AI, preserving the exact current map package and collecting timestamp-matched `exception.log`, `lua.log`, `debug.log`, launcher output, slot/faction/options, and seed. Determine whether the fault is in the map manifest/rules/scripts, player/slot setup, Skynet module initialization, unsupported actor/prerequisite data, or shared engine code; fix the smallest correct owner rather than hiding the error or disabling bots. The literal gate is that Empire Lars loads into active play with a human and Skynet, both actors/world and real AI modules advance normally, and no fatal/Lua/unhandled error occurs. Test additional slot positions, human faction/observer variants, Skynet alone and with ordinary AIs, save/load where applicable, and at least one other official map to prove no regression. If the map is user-supplied rather than tracked, preserve a reproducible test copy outside Git or under ignored logs and document its checksum/location; only add/modify distributable map content when licensing and repository ownership are clear. |
| CNC-26B | complete (8 cycles; PR #53 green) | **Iron Reaper branch-config parity (first pass).** Iron Reaper currently performs poorly while using the covert branch because it lacks important VIKI configuration and optimizations. Give covert Iron Reaper the configured ability and demand to build stealth tanks, stealth harvesters, and stealth generators; use nuclear weapons; and construct/place stealth generators as VIKI does. Prefer sharing or selecting the native branch configuration instead of copying values into another divergent configuration: VIKI behavior/config is the acceptable first-pass source for covert, Skynet for recon, and Brutalis or existing Iron Reaper behavior for economy. Preserve Iron Reaper's switching/all-technologies mechanisms and ensure a branch change selects the appropriate production and support configuration without altering the native AIs. Test that each required covert asset and nuclear behavior is actually exercised in real matches, and compare covert Iron Reaper with VIKI under equivalent technology and opponent conditions. |
| CNC-27 | complete (15 cycles; PR #54 green) | **Stealth-tank squads.** Generalize relevant air-squad logic into two to three stealth-tank attack/harassment squads while leaving roughly half of stealth tanks in normal armies. Attack squads cooperate against tanks. Harassment groups begin around two tanks, grow late-game for rapid fact kills, prioritize harvesters/advanced power, out-range lone tanks, crush infantry, avoid defenses/detectors/retaliation, and only clear defended areas carefully. |
| CNC-28 | complete (10 cycles; PR #55 green) | **Stealth chemical tanks.** Config only: give chemical tanks the same stealth model as stealth tanks, including decloaking on fire and near detector types. |
| CNC-29 | complete (13 cycles; PR #56 green) | **Stealth chemical squad.** Add one stealth chemical harassment squad using the stealth-tank framework, while leaving roughly half of chemical tanks in normal armies. Prioritize clustered infantry, harvesters, and Tiberium-armor buildings; never deliberately target tanks. |
| CNC-30 | complete (19 cycles; PR #57 green) | **Crates and exploration.** Pick up only supply crates currently visible to the AI. Ground units use ordinary accessible paths; aircraft may land on and collect crates using the same alternate/AltGr move command available to players, with a valid safe landing approach. If the AI lacks an MCV and/or money, use all suitable mobile units to seek visible crates; otherwise send distinct scouts toward accessible 6x6 cells never seen or seen least recently. Sleep roughly ten seconds between scans. If it has multiple buildings but no mobile unit, sell a nonessential building such as a power plant to create a scout. Keep scans bounded and deterministic, avoid assigning several collectors to one crate, and test blocked ground access plus aircraft landing/pickup. |
| CNC-31 | complete (30 cycles; PR #58 green) | **Red-Tiberium bomb trucks.** When stealth harvesters and red Tiberium are available, load actual red Tiberium and drive selected harvesters into distinct high-value enemy-base targets such as facts, HQ/eye/advanced communications/temples, refineries, war factories, and airfields. Limit launches to about 5% of total harvesters per minute. |
| CNC-32 | complete (15 cycles; PR #59 green) | **Remove unsupported mods.** Ensure normal solution, build, test, packaging, and autonomous workflows exclude Red Alert, Dune 2000, and Tiberian Sun. Preserve shared engine compilation needed by CNC. |
| CNC-33 | first iteration (30 cycles; PR #60 green) | **Smart economy.** Build refineries, or silos where appropriate, when many harvesters wait to unload. When cash is excessive, increase base expansion, MCVs, and combat-production capacity until the imbalance is corrected. Prefer measured queue/wait evidence; use bounded proxies only when necessary and document them. Golden comparison gate: with otherwise identical AIs and an economy-heavy scenario that exercises the feature, the smart-economy AI must decisively outperform the feature-disabled control consistently in both early survival/scaling and later-game outcome, measured through sustained income/spending, shorter unload congestion, useful production/expansion capacity, army/economic value, and match outcome—not merely logs or one additional structure. Use well-separated spawns so the game naturally runs long enough to measure both phases. Include a native VIKI differential: VIKI has no silos, so unserviceable storage pressure must never stall its build queues or prevent it from converting large cash balances into useful production, expansion, and army value. |
| CNC-33A | first iteration (33/33 cycles; draft PR #61) | **Smart-economy refinery throughput follow-up.** Detailed result: `AUTONOMOUS-CNC-REPORTS/CNC-33A.md`. Uncapped refineries, the shared 90 direct/free-harvester target, literal-zero recovery, the 10K differential, save/load, and SkyNet regression pass; the final locked-faction VIKI differential still loses. |
| CNC-24.7 | complete (27 cycles; PR #64 green) | **Run independent headless MAX simulations concurrently.** Add a repository-owned Linux-capable orchestration path that can launch, monitor, summarize, and stop two or three independent full-engine games at once on a four-vCPU host, while retaining one-game serial mode. Give every process an isolated support directory, settings, logs, replays, saves, benchmark prefix, map artifact, display, and any network endpoint; never share mutable runtime files or let one crash/hang conceal another result. Poll each run within the autonomous 60-second update bound, preserve per-run exit/fatal/tick/bot/map/outcome evidence, and support bounded termination without orphan processes. Benchmark identical representative workloads serially, two-wide, and three-wide; accept concurrency only when it increases valid completed simulation work per wall-clock time without material tick starvation, memory pressure, evidence corruption, or changed simulation rules. Test natural completion, bounded stops, one failing sibling, save/load, repeated batches, cleanup, and compatibility with the existing single-game launcher. |
| CNC-33A.1 | complete (30 cycles; draft PR #65 green) | **Prioritize early vehicle-production scaling over refinery throughput.** Audit the previous configurable opening/build-order work and verify whether its intended combat/economy construction split was 50/50 rather than assuming it survived later changes. Except while recovering from literally zero unloading refineries, refinery work from every source combined must occupy no more than half of active Fact construction; while early vehicle-production capacity is inadequate and a war factory/airfield is viable, prioritize that capacity because it scales both combat and harvester production. Keep refinery totals uncapped over time, retain the shared 90 live-plus-queued direct/free-harvester target, and do not leave Facts or cash idle when vehicle production is unavailable, blocked by prerequisites/power, or already adequate. Test matched smart/control VIKI games on ordinary maps and on smaller, resource-scarce maps with low starting cash and limited nearby Tiberium that specifically expose economy over-prioritization. Measure refinery versus production-building starts, time to useful vehicle capacity, queue occupancy, spending, harvester growth, income, army/assets, and match outcome; retain the 10K many-prebuilt-production success, literal-zero recovery, save/load, and SkyNet regressions. |
| CNC-33A.2 | first iteration (30 cycles; draft PR #67 green) | **Independent red/blue Tiberium explosion lobby options.** Semantic world, weapon, delayed-warhead, field, actor-trait, and cargo gates plus the full four-option matrix, red field/cargo paths, ordinary-damage regression, and a natural tick-45,000 match pass. An isolated custom-map reload and a clean blue loaded-cargo observable remain unproven; detailed result: `AUTONOMOUS-CNC-REPORTS/CNC-33A.2.md`. Follow-up regression/semantic revision: CNC-86. |
| CNC-33A.1.1 | first iteration (30 cycles; draft PR #68) | **Hard-AI deterministic openings and matchup progression.** Give VIKI and Iron Reaper SkyNet's configured opening structure order except for an opening helipad. Give Brutalis the same opening but place a second airfield, with sufficient power in between, before its first radar. SkyNet, VIKI, Iron Reaper, and Brutalis must rapidly reach five total live-plus-queued harvesters and then request exactly one opening MCV; after those goals, normal build logic may take over and may use idle queues not needed by the current ordered goal. Prevent discretionary structures or units from consuming critical opening cash: specifically reproduce and forbid VIKI buying three barracks and starving at one harvester. Smart economy may control refinery need/reservations but must not replace preferred opening goals or the original configured chance/fraction-and-limit selector for other structures. Make VIKI reliably build its HQ, reach available covert/stealth technology instead of stalling below tech, then use stealth-tank squads to suppress or eliminate exposed enemy harvesters. Prefer nearer safe prey with stronger distance/path cost, and when no safe target is immediately available wait near Tiberium fields where harvesters are likely to return; do not use fixed hunting-ground partitions. Avoid routing or parking on blue Tiberium while engaging, especially during active explosions, and avoid crossing ordinary firefights: lone-infantry crushing, tank sniping from outside return range, and hit-and-run harvester attacks are the intended safe patterns. If every useful target remains defended for a sustained interval, stop waiting indefinitely and cautiously clear the weakest viable defender first, adapting the established air-squad safety and defender-selection model to stealth tanks' ground threats and targets. In a long-distance VIKI-versus-two-Brutalis regression where Brutalis lacks adequate detection, VIKI should decisively dominate. Iron Reaper must resume SkyNet-like post-opening helipad/aircraft production against covert VIKI. SkyNet itself must scale beyond one helipad when its air fleet needs it and repair aircraft on a pad concurrently with pad production instead of queueing the army behind one facility. Audit native/original configs and compare directly with the pre-Codex random planner; the new bots must consistently outperform that benchmark, and a loss is evidence of a wrong assumption rather than an acceptable random result. Test faction alternatives, random choices, low cash/income, blocked prerequisites, parallel queues, losses/retries, save/load, air production/repair contention, and complete matches. |
| CNC-33A.3 | first iteration (30 cycles; draft PR #69) | **No-mutants lobby option.** Add a lobby option that prevents all mutant creation for the entire match, including viceroids and mutation caused by Tiberium fields, cargo, weapons, deaths, scripts, or red-Tiberium detonation. When red Tiberium would otherwise use its mutating red explosion and red explosions remain enabled, substitute the equivalent existing blue-Tiberium explosion behavior so the detonation still occurs but cannot create mutants; the independent no-red-explosions option must still suppress it completely. Preserve default mutation behavior when disabled and test all combinations with no-red/no-blue explosions, existing mutants at match start, delayed effects, ownership changes, save/load/replay, AI and human detonations, and complete real matches. |
| CNC-30.1 | complete (20 cycles; draft PR #70 green) | **Bound normal exploration and correct map coverage.** When the AI has no urgent crate-recovery need, cap simultaneous explorers at ten infantry and use only rifle infantry, flamethrowers, or chem troopers. Prefer chem troopers, then flamethrowers, with rifle infantry the least desirable because chem troopers cross Tiberium safely and can kill encountered harvesters. Preserve the existing urgent missing-critical-asset crate behavior. Diagnose and fix the observed directional bias where exploration repeatedly favors the top of the map while leaving areas such as the bottom-right corner unseen; require bounded, deterministic, map-wide stale-cell selection without concentrating explorers in one region. Test ordinary and urgent modes, unavailable unit types, fewer/more than ten candidates, reservations/contention, shroud history, asymmetric/blocked maps, all corners, save/load, and complete real matches. |
| CNC-33B | complete (30 cycles; draft PR #71 green) | **Decloak and explicitly deploy unstable red-Tiberium harvesters.** When a cloaked/stealthed harvester carrying unstable red Tiberium enters the existing warning/blinking state before detonation, it must visibly decloak for the full warning period and regain normal cloak behavior only after the unstable cargo/warning condition ends. Even when the no-red-Tiberium-explosions lobby option is enabled, a red-Tiberium-carrying harvester may be manually detonated with a deploy order only after carrying the unstable cargo for at least two minutes and blinking visibly while decloaked for the final 30 seconds; deployment before either boundary must do nothing. The red-Tiberium bomb-truck AI module must issue this deploy order at a valid target after both timers, and covert-technology Iron Reaper must be verified to enable and use that module. First determine whether the existing blink condition can drive the cloak trait entirely through rules configuration; prefer a config-only implementation when it preserves exact timing, and add the smallest deterministic code condition/order bridge only if needed. Do not alter ordinary green/blue cargo, stealth detection, non-harvester cloaking, or default automatic red-Tiberium behavior when the lobby option is off. Test loading, both exact timer boundaries, early deploy rejection, warning/decloak, unload or cargo loss, successful deploy, entering/leaving detector range, ownership/technology changes, VIKI and covert Iron Reaper AI missions, save/load mid-timer and mid-blink, all relevant lobby-option combinations, and complete real matches. |
| CNC-34 | complete (30 cycles; draft PR #72 green) | **First economy tower.** Keep the working opening system, but make the first defensive tower for a GDI/economy-branch Skynet an Obelisk rather than a SAM site when buildable, protecting the fact from early rushes. |
| CNC-35 | complete (30 cycles; draft PR #73 green) | **General attack squad.** Generalize air-squad concepts into cohesive mixed ground attack squads excluding aircraft and incoming reinforcements from the formed center. Use bounded 6x6 strategic cells, slowest-unit travel cost, harvester-first priorities with buildings and defenseless artillery high, and defender value versus target value. A 5:1 attacker-value advantage makes defense effectively negligible, not invisible. Preserve existing defense squads. Let air squads mark high-value AA targets for extra ground-force priority. |
| CNC-36 | complete (21 cycles; draft PR #74 green) | **Economy artillery squad.** Create one economy-branch MRLS artillery cluster. Assign at least one MSAM and at most about 5% of cluster value as MSAMs, plus about 5% medium tanks and 5% riflemen as defenders. Keep range, bombard the nearest/useful turret or blocker, scout impact areas if needed, retarget defense when a guarded MRLS dies, and prevent empty MRLS from advancing. Nod artillery remains in normal forces. |
| CNC-37 | complete (17 cycles; draft PR #75 green) | **Covert harassment.** Build a fast covert harassment squad around recon bikes and buggies with some artillery support. Only bikes/buggies define its center. Wait for artillery only against towers; otherwise let slower artillery/reinforcements catch up and reserve them for heavy attacks. |
| CNC-38 | complete (18 cycles; draft PR #77 green) | **Early VIKI infantry rush.** Early-game VIKI sends individual chemical troopers against harvesters, refineries, and power, and forms two ten-grenadier base-rush squads. Maintain spacing so grenadiers do not walk into their own attacks (delay forward movement after kills) and keep distance from enemy units. |
| CNC-39 | complete (8 isolated + 2 RC2 integrated cycles; PR #84 RC4; strict final-review evidence `games/rc2-final-review-response-01`) | **Engineer correction.** A lone engineer may capture an enemy building below 80% health (rather than the old 50% threshold). Revalidate five-second/value-distance reassessment, valuable husks, coordinated healthy-building captures, and pairing without stale long routes. |
| CNC-39A | first iteration (11 isolated + 0 integrated cycles; PR #84 RC4; evidence `analysis/worker-2-cnc-39a/cycle-11/`; remaining gaps: exercised ordinary transport takeover, defended natural-production match, direct UI force-target test) | **Engineer/commando target coordination.** Prevent engineers and commandos from entering or acting on the same building concurrently. Share deterministic target reservations between capture and demolition assignments, and revalidate ownership/relationship when a queued C4 order executes so a building captured in the meantime can never be detonated after becoming friendly. Test both simultaneous selection and capture-during-commando-travel races. |
| CNC-43 | complete (1 isolated + 0 integrated cycles; PR #84 RC4; evidence `analysis/worker-3-cnc-43/games/integrated-rc2-{three-way,two-way,enclosure-recovery}`) | **MCV crush flavor.** Config only: give MCVs the Mammoth tank's crush capabilities. Do not change AI behavior or unrelated balance. |
| CNC-43A | complete (2 isolated + 0 integrated cycles; PR #84 RC4; evidence `analysis/worker-4-cnc-43a/{final-literal-v1,true-tanks-v1,natural-terminal-v2}`) | **Flame Tank balance.** Config/content only: increase Flame Tank health by 20% and increase its damage against tank armor by 10%. Preserve its damage against every other armor class and avoid unrelated unit or AI changes. Validate the exact before/after health and versus-tank damage values. |
| CNC-51 | complete (14 isolated + 1 RC3 integrated cycle; PR #84 RC4; evidence `AUTONOMOUS-CNC-LOGS/cnc51-rc3/{run-53-terminal-loaded-recovery-pass-3,run-54-final-literal}`) | **Transport-helicopter unload recovery and threat-safe landing.** Rescue, assault, and heavy-drop helicopters must not stall because an actor occupies the requested landing/unload cell. Before unloading, deterministically search nearby for an accessible cell with valid adjacent passenger exit space; it need not be the exact destination. Because landed helicopters are susceptible to every applicable damage type, evaluate each stopping/landing cell and its exit area against all live enemy weapons able to damage the carrier at landing altitude—not only actors classified as AA. This combined landing threat must include Mammoth tanks as well as MSAMs, use true/veterancy range and mobile-threat movement margins, and never use a fly-by discount. Transport attack approaches must demonstrably use both the threat-aware router and the coarse strategic map to select a safe approach vector and landing area instead of flying directly to a target cell. Re-evaluate when actors, structures, or new threats block/compromise the site; route to another safe unload cell or hold/withdraw instead of landing beside an MSAM, Mammoth, or equivalent threat. Add timeout/stall/unsafe-site diagnostics that log rejected threats and whether strategic routing was used. Test simultaneous transports, dense friendly units, structures, map edges, targets blocked in flight, newly arriving mobile/static AA, Mammoth cannons/missiles, and threats covering only one approach vector. The preserved manual log records three rescue missions timing out while still carrying cargo; a later Archipelago test visibly landed carriers beside both an MSAM and enemy Mammoth tanks. |
| CNC-40 | in progress — claimed for coordinated round 20260807-bug-polish-02 | **Adaptive specialists.** Extend adaptive production evidence so commandos receive credit for the economic value of buildings detonated and engineers receive credit for captured buildings and restored husks. |
| CNC-41 | in progress — claimed for coordinated round 20260807-bug-polish-02 | **Economy Tiberium fields.** Build one resonator per Tiberium tree. For red trees, first enclose the tree/resonator at four cells using the best walls/sandbags, leaving an entrance occupied by an accessible building edge or repair pad; extend build radius with power at roughly six cells when needed. Repair incomplete walls every 60 seconds. Prove harvest access and containment on a validated red-Tiberium map such as Empire Earth/Empire Red. |
| CNC-42 | in progress — claimed for coordinated round 20260807-bug-polish-02 | **Economy field defense.** Mid/late game, station roughly one medium tank, two riflemen, and one MSAM per harvester near the field from its saved last-harvest point. Update the station only after unloading; keep infantry out of Tiberium and guards out of refinery traffic. Also use SAM sites to defend economy structures. |
| CNC-44 | in progress — claimed for coordinated round 20260807-bug-polish-02 | **Aircraft husks.** Make aircraft create capturable husks when they crash (Orcas and helicopters already fall to ground), except A10s. Resolve CNC-62 crash damage before the occupancy decision and inspect every actor sharing the impact cell: create the husk only when no blocking actor survives. One occupant dying is insufficient if another survives. Prefer the crash squishing/killing all infantry in the exact cell so stacked infantry do not prevent the husk; a surviving vehicle, building, or other blocking actor still prevents it. Capturing the husk grants the aircraft. Test empty cells, one and multiple infantry, mixed infantry/vehicle occupants, partial survivors, crash-killed occupants, simultaneous removal timing, terrain/map edges, and every husk-capable aircraft type. Related user history (2026-08-07): AI Engineers may not recognize newly added aircraft husks as capturable/restorable; preserve this report for the separately queued CNC-97 regression without changing this active contract. |
| CNC-45 | in progress — claimed for coordinated round 20260807-bug-polish-03 | **Economy troop production/use.** Make economy armies primarily Mammoth tanks with riflemen and the artillery squad; use medium tanks, not Mammoths, for harassment. Occasionally give Mammoths bounded crush orders and make their approach distance account for the shortest-ranged usable weapon so cannon damage is not wasted. Preserve ordinary behavior and CPU performance. |
| CNC-46 | in progress — claimed for coordinated round 20260807-bug-polish-03 | **Defense clusters.** Mark an attacked tower as the active cluster and build defense near attacked structures. Wall active towers without blocking base access, sell walls that block movement/construction. Add at least three nearby towers and at least one of each available role, plus a repair pad behind the cluster where it can cover some towers/walls. Deprecate old non-MCV straight wall construction. |
| CNC-47 | in progress — claimed for coordinated round 20260807-bug-polish-03 | **Repeatable performance baseline.** Create a repeatable late-game test on Normal and Fastest with at least five active AIs and, where feasible, 300+ mobile units per AI. Record real/game-time ratio, actor counts, simulation timing, and profiler evidence. Exercise scaling and adversarial cases. Only make bounded behavior-preserving improvements here; never lower the 300-unit floor to manufacture gains. Related user history (2026-08-07): intermittent/periodic freezes were reported on very old hardware; preserve this report for the separately queued CNC-96 stall-diagnosis regression without changing this active contract. |
| CNC-48 | pending | **Integration test.** Test cumulative behavior and lag with Skynet recon, VIKI covert, Brutalis economy, and technology-switching Iron Reaper. Expected counters: Skynet beats VIKI, VIKI beats Brutalis, Brutalis beats Skynet, and Iron Reaper should beat all through switching. Older AIs may use general intelligent behavior but retain their one-MCV and individual-harvester limits. Record any lag source in `DEFERRED_WORK.md`. |
| CNC-49 | pending | **Lag reduction.** Use the performance baseline, integration report, and deferred-work findings to measure before/after performance at fixed unit counts. Preserve deterministic replays. Prefer algorithmic/event-driven improvements and spreading recalculations; with hundreds of units lengthen low-value intervals, then only if still necessary fall back to simple valuable-area mass attacks, never below 100 total units. Inspect legacy per-tick scans plus stealth/repair-area code, and remove obsolete debug logging/log files. |
| CNC-50 | in progress — claimed for coordinated round 20260807-bug-polish-03 | **Late-game engineer stall recovery.** Investigate engineers that remain idle despite nearby capturable vehicle husks or buildings, across every AI type and especially after owners/targets die in the late game. Revalidate assignments periodically and whenever an engineer has no valid order; release stale target reservations, rank newly available local husks/buildings normally, and recover rather than remaining stopped until death. Add bounded diagnostics that explain why visible candidates were excluded. Preserve and use the manual-game evidence in `AUTONOMOUS-CNC-LOGS/manual-post-cnc25-20260803-213129/`; it contains repeated late-game `no eligible solo target` stops. Coordinate with CNC-39/CNC-39A rather than duplicating or regressing their value, pairing, and reservation rules. |
| CNC-52 | in progress — claimed for coordinated round 20260807-bug-polish-03 | **Starting-Fact wall hole prevention/repair.** For only the first deployed MCV/Fact and only during the first five game minutes, prevent avoidable friendly construction from consuming its planned wall cells or periodically repair incomplete wall-ring cells as well as map/build-radius constraints allow. Do not wall later Facts and stop maintenance after five minutes. Treat an occupied ring cell intelligently and repair it if it later becomes available. Preserve access and leave general wall self-blocking/selling behavior to CNC-46. The preserved manual log records Brutalis 2 abandoning its starting enclosure after eight blocked attempts. |
| CNC-53 | pending | **Transport-helicopter passenger batching.** Use Chinook capacity efficiently when several compatible infantry need transport toward the same or nearby destination. A single urgent isolated route-failure rescue may depart alone, but an attack/assault transport must never launch with only one ordinary infantry unit. Non-urgent missions briefly and deterministically collect nearby compatible passengers up to the helicopter's actual capacity (currently ten infantry), with a configurable minimum attack load and bounded wait so transport does not become counterproductive. Do not combine passengers with conflicting destinations or specialist missions, steal reserved units, exceed cargo weight, or regress emergency unloading and safe routing. If the attack minimum cannot be met, cancel/release the mission rather than performing a one-soldier attack. Coordinate landing-cell recovery with CNC-51 and test urgent single rescue, forbidden single attack, partial/full attack loads, mixed passenger weights, competing reservations, and passengers appearing during the gathering window. An Archipelago test visibly reproduced repeated one-infantry attack flights. |
| CNC-54 | pending | **Protect allied-aid supply trucks.** A supply truck requested and built for CNC-23 allied recovery must be reserved for its intended ally mission instead of being claimed by the producer's ordinary self-supply behavior. Carry the deterministic aid intent from production request through actor creation and delivery; self-supply managers and squads must ignore the reserved truck. Cancel queued production when possible if aid is no longer needed, and define a useful bounded fallback for an already-built truck when the ally recovers or becomes ineligible. Test competition with simultaneous self-supply demand, multiple factories/allies, ally recovery during production/travel, destroyed destinations, and ownership changes. Log why each aid truck was requested, reserved, redirected, delivered, or released so wasted queue use is visible. |
| CNC-55 | pending | **Cash-starved economy bootstrap.** Enter emergency recovery when the AI has no functioning income source and spending on anything other than the missing refinery/harvester path would leave it unable to afford recovery. Derive the threshold from live actor costs and prerequisites rather than a fixed cash number. Choose exactly one mutually exclusive direct objective: if usable refinery capacity and vehicle production exist but no harvester does, reserve/prioritize only a harvester; otherwise reserve/prioritize the earliest viable refinery plus only its strictly required power/build-radius prerequisite. Never request both paths concurrently when the budget cannot complete both. On entry, cancel active unrelated construction/production orders, clear their ongoing queue entries and associated reservations/build locks, stop all paid repairs, and pause walls, defense, infantry quotas, technology, and normal scaling; this does not mean deleting diagnostic logs. If direct recovery is unaffordable, prioritize the smallest viable engineer package and coordinate CNC-50/CNC-59 capture logic toward the missing income asset—prefer useful harvester husks, then Facts/construction capability or refineries according to reachable economic recovery value—while also allowing CNC-30 visible-crate exploration and bounded nonessential-building sales. Recovery utility must outrank superficially valuable but unnecessary capture options. Revalidate ownership, target availability, affordability, and queue state frequently enough to recover from failed captures without spamming engineers. Once viable harvesting income resumes, release emergency reservations, restore ordinary queues/repairs deterministically, and continue the prior opening rather than remaining locked in recovery. Do not trigger while an existing refinery/harvester income path is functional. Required focused game test: give a cash-starved AI only one to four engineers plus many reachable capture choices, including distracting assets it does not need and at least one valid route to a harvester husk, refinery, or Fact; the test passes only if it selects enough recovery-critical assets to restore income and then resumes normal production. Also test low/zero starting cash, refinery-only and harvester-only loss, both missing, budget sufficient for only one recovery path, stale queued buildings, active repair drain, missing prerequisites, unreachable/contested husks and buildings, unavailable refinery technology, allied aid arriving, crates, captured recovery assets, failed engineers, and transition back into the normal build order. |
| CNC-56 | pending | **Simplify allied cash aid.** Replace CNC-23's complex eligibility and five-minute truck limits with one simple asset gate plus cash: if an ally simultaneously owns a Fact, at least one live harvester, and at least one live refinery, the 5000-credit aid rule does not apply and no recovery truck is requested merely because its cash is low. Otherwise, when the ally has less than 5000 cash, build and deliver supply trucks until it reaches at least 5000. Re-evaluate the three essential assets after production, losses, captures, and ownership changes; stop/cancel aid promptly when all three are restored. If delivered cash remains unspent for a bounded configurable observation period, treat that as evidence the ally cannot currently use further aid and suspend deliveries; resume only after meaningful spending or asset-state change. Preserve CNC-54 mission reservations so aid trucks cannot be consumed by the donor. Keep requests deterministic and avoid excessive simultaneous queue commitments. Test every combination of missing/present Fact, harvester, and refinery; a fully equipped ally below 5000; cash just below/above 5000; active spending; permanently unspent aid; multiple donors/recipients; ally defeat/ownership change; delivery loss; and recovery after suspension. Remove obsolete eligibility/rate-limit branches rather than layering the simpler policy on top. |
| CNC-57 | pending | **Supply-truck production stall recovery.** Diagnose and fix AIs that stop building supply trucks while valid self-supply or allied-aid demand remains. Ensure stale production intents, destroyed/replaced factories, rejected queue requests, reservations, cooldowns, and completed/lost trucks cannot permanently suppress later requests. Add bounded reason/state logging from demand through queue selection, actor creation, mission assignment, delivery, loss, and retry. Coordinate with CNC-54/CNC-56 so the simplified aid policy may remove the underlying failure, but independently prove production resumes after every failure state and does not spam trucks without demand. |
| CNC-58 | pending | **Scalable coordinated heavy drops for normal and captured/granted forces.** Any AI—including Brutalis and Skynet—that owns compatible Mammoths/heavy units and transport helicopters should be able to form the CNC-25 coordinated heavy-drop strategy even when its normal production strategy would not build Mammoths. Ten matched pairs remain the preferred full wave, but earlier or supply-limited drops may launch with a configurable smaller force when no more units/carriers are available within a bounded gathering window and the available force-value versus candidate landing-cell defense is still viable. Reserve a planned drop's heavy units and carriers before generic one-unit route-failure rescue or infantry transport can claim them; those systems may use only unreserved leftovers. If a full wave loses carriers during assembly, recompute and launch a safe viable reduced wave rather than waiting forever or letting rescue missions consume the formation. Select a valuable threat-safe enemy-base landing area, travel cohesively, unload, and hand off a decisive ground attack. Do not change Skynet's ordinary production merely to force this strategy or launch a token force into a defended cell. Test Archipelago with Brutalis and Skynet, matched forces of several smaller sizes plus 10+, inaccessible enemy land, competing rescue/infantry observations, mobile/static landing threats, partial losses during assembly, and post-drop attack handoff. Preserved logs show Brutalis enabled heavy drop with 10 Mammoths but generic rescue reduced available carriers from 9 to 5, preventing launch; the earlier Skynet test created 21 individual rescue missions. |
| CNC-59 | pending | **Dynamic neutral-building capture demand and specialist transport.** Detect capturable neutral structures from actor traits/relationships at runtime—without hard-coded derrick, hospital, biolab, or map-name lists—and create engineer production demand while useful neutral targets remain unclaimed. Compare targets with live, queued, reserved, and travelling engineers so production is sufficient but bounded; rank assignments by economic/strategic value, recovery utility, distance, accessibility, and transport feasibility. Neutral strategic structures should outrank unnecessary healthy-enemy pairing or distant husks when they offer the better attainable result, and neutral ownership rules must not incorrectly require the same engineer count as an undamaged hostile building if the game allows a lone capture. When the chosen target is in another movement domain, reserve the required engineer(s), a compatible helicopter, and a CNC-51-safe landing cell as one specialist capture/rush mission; do not wait for generic route-failure rescue, which may exclude engineers. After unloading, retain the capture target and re-evaluate ownership rather than stalling. Re-evaluate when ownership, reachability, production prerequisites, or reservations change, and return engineer weighting to normal once targets are claimed or impossible. Test one to ten granted engineers, multiple neutral actor types, distracting hostile buildings/husks, no neutrals, inaccessible islands, competing allied engineers, destroyed/captured targets, missing barracks, and Archipelago oil derricks. The latest preserved test assigned engineer pairs and a distant husk but logged no successful neutral captures and no engineer transport missions despite ten idle Chinooks. |
| CNC-60 | pending | **Dynamic last-Tiberium-patch conservation.** Without map-specific rules, identify connected Tiberium patches within each harvester's navigable movement domain. Before a harvester consumes from the last remaining patch in its domain, suspend harvesting and park all of that AI's harvesters at safe nonblocking locations for five game minutes so the field can regrow; suppress ordinary harvester orders during the rest period, then deterministically rescan and resume or rest again. When this conservation sleep triggers and the AI has access to Tiberium-resonator technology, give resonator construction emergency priority near any viable Tiberium patch. If the field is outside current build radius, construct the minimum deterministic chain of suitable power plants/build-radius providers needed to reach it, then place the resonator without blocking harvest access. Reuse/coordinate with CNC-41's resonator, red-field wall, and build-radius placement rules rather than creating a competing implementation; if the technology or route is unavailable, continue the rest policy without stalling other construction forever. Cache/bound resource and movement-domain scans instead of searching the whole map every tick. Do not trigger while another usable patch remains in the relevant domain, confuse unreachable Tiberium with available supply, block refinery traffic, overbuild duplicate resonators/extension structures, or leave harvesters permanently asleep after ownership/domain/resource changes. Test Archipelago scarcity, one versus multiple patches, separated islands/domains, the final cell of a patch, patch regrowth, resonator technology available/unavailable, remote fields requiring power-plant expansion, destroyed extension buildings/resonators, destroyed/sold refineries, newly spawned resources, captured harvesters, and several AIs sharing a field. |
| CNC-61 | pending | **Silo and stealth-generator build radius.** Config/content only: give every buildable Tiberium silo a normal base-building placement radius. Inspect every stealth-generator actor/variant and confirm it already provides build radius; add the equivalent trait/config only where missing. Use the established radius convention for comparable small support structures rather than introducing AI code or map-specific exceptions. Validate rules/YAML, exact affected actor variants, construction extension from each structure, and that unrelated stealth, storage, power, prerequisites, cost, and balance remain unchanged. |
| CNC-62 | pending | **Aircraft crash-damage increase.** Config/content only: for Apache/helicopter, Orca, A10, and transport-helicopter crashes, set impact damage to exactly six times the transport helicopter's current pre-task crash-damage value, using that captured baseline consistently rather than multiplying each aircraft's possibly different value. Apply the impact to every applicable actor sharing the exact crash cell rather than stopping after one; where rules support it, lethally squish infantry in that cell. Preserve flight, death animation, fall behavior, targeting, and unrelated weapon damage. Coordinate with CNC-44 so damage resolves before husk occupancy: a husk-capable aircraft may leave a husk only when no blocking occupant survives. Validate exact raw and armor-adjusted damage for all listed aircraft, empty cells, stacked infantry, mixed infantry/vehicle occupants, friendly and enemy occupants, partial-survivor versus fully lethal outcomes, and YAML/rules lint. |
| CNC-63 | pending | **Transport-helicopter passenger render order.** While an actor is approaching and entering a transport helicopter, always render the passenger beneath/behind the helicopter sprite until cargo entry removes it from the world. Make this deterministic for every approach direction, facing, subcell, terrain height, and supported passenger type without changing gameplay altitude, collision, targeting, selection, or unrelated global render ordering. Restore ordinary actor ordering after unloading. Test north/south/east/west and diagonal approaches, several simultaneous passengers, moving or turning carriers, interrupted/cancelled entry, carrier destruction during boarding, and unload/reboard transitions. |
| CNC-64 | pending | **Veteran-first transport allocation and adaptive Chinook capacity.** Prefer the highest-veterancy compatible units for strategic drops when alternatives exist. The faster transport helicopter should travel to and land near its assigned passenger for pickup; only a short final boarding move by the ground unit is acceptable. When carriers are busy, prioritize more-veteran waiting passengers among otherwise comparable transport requests, while preserving urgent rescue/specialist missions, existing reservations, compatible destinations, and deterministic tie-breaking. Keep 10 transport helicopters as the normal target, but when a bounded moving utilization/backlog signal shows sustained saturation, allow low-priority production to grow the fleet adaptively to a hard ceiling of 20; stop extra demand when the backlog clears. Count live, queued, loaded, reserved, repairing, and destroyed carriers correctly and avoid production spam. Test mixed veterancy, equal-veterancy tie-breaking, distant and inaccessible passengers, busy/repairing carriers, short versus sustained saturation, queue contention, and both the normal 10 and hard 20 ceilings in real matches. |
| CNC-65 | pending | **Granted-force APC formation and universal opportunistic transport.** The roughly 50% strategy roll controls only whether an AI deliberately emphasizes the early APC-rush production/build plan. Independently of that roll, every AI that already owns usable APCs and compatible infantry should batch them for faster useful long-distance normal attacks or engineer/commando infiltration instead of walking the passengers across the map. Reproduce an ordinary AI starting with one APC and ten infantry: it must recognize and reserve the granted actors promptly, load a viable group, drive toward a useful destination, and unload under the existing safety/early-contact rules. Preserve genuinely needed local defenders, passenger/destination compatibility, specialist intent, normal squad ownership, and deterministic reservations; do not launch token one-infantry ordinary attacks or force extra APC production outside the selected strategy. Release reservations and fall back safely if the APC, passengers, or route become invalid. Test the literal 1-APC/10-infantry start with full normal modules and both opening-roll outcomes, several factions/infantry mixes, normal attack versus capture/C4 destinations, delayed actor discovery, competing defense demand, blocked loading cells, APC loss, and a completed real match. |
| CNC-65A | pending | **Full, useful APC-rush passenger composition.** The existing APC rush can launch partly empty and without specialists, producing an ineffective attack. Do not deliberately launch a rush until every usable cargo slot/weight unit in the APC is filled, except that the existing seen/shot-at emergency behavior may unload or abort immediately. Select passengers deterministically by mission value, prioritizing engineers, commandos, grenadiers, chemical troops, rocket/bazooka troops, and flame troops; ordinary riflemen are last-resort filler for a non-engineer assault rather than preferred cargo. An APC carrying any engineer is a protected specialist mission and may contain only engineers, commandos, and rocket/bazooka troops, because grenadiers, chemical troops, flame troops, riflemen, and other ordinary assault infantry may accidentally kill the engineers. Keep destinations compatible with the resulting capture/C4/assault composition, do not steal required base defenders or already-reserved specialists, and release reservations if a legal full load cannot be assembled within a bounded wait. Test full and partial candidate pools, cargo-weight variants, every preferred passenger type, engineer-safe mixed loads, forbidden engineer combinations, several competing APCs, passenger loss during assembly, early contact, and post-unload mission handoff in full real matches. |
| CNC-67 | pending | **Faster, stronger adaptive production and sheep-spam benchmark.** Raise the configurable maximum composition share for one learned/adaptive unit type to 75%, including the sampled-unit path whose current combined ceiling is 50%, and increase the learned-unit response rate fourfold from the current 5% initial sampled chance to 20%. Keep an explicit configurable floor and ceiling, preserve economy/MCV emergency gates, and avoid interpreting one lucky early result as permanent evidence. Ensure adaptive selection evaluates every compatible production queue in parallel and exposes demand to construction logic so the AI prioritizes additional production buildings for the currently wanted unit/queue type instead of leaving useful queues absent or idle. Add decision logging for observed composition, learned weights/shares, queue offers, affordability, selected production, and production-building demand. Create a deterministic sheep-specialist test AI limited to 20 harvesters and unable to build Tiberium resonators: it follows an ordinary opening for five game minutes, then produces only sheep. Test against normal AIs in complete real matches. The gate is not merely that sheep are learned: the normal adaptive AI must increase sheep production toward the 75% ceiling, build/use the necessary queues, and decisively defeat the sheep specialist through adaptation while still passing controls against ordinary opponents and recovering when the observed composition changes. |
| CNC-68 | pending | **Cached generalized pairwise combat-threat calculator.** Build a deterministic reusable helper that derives the combat exchange relationship between every relevant actor-type pair from rules/config data once per game and stores it in a fast lookup table keyed by an unambiguous ordered pair such as `(attacker type, defender type)`. Store both directions explicitly: X's threat to Y and Y's threat to X. Include target validity, each side's HP and armor type, applicable weapon damage and `Versus` modifiers, burst/reload/rate of fire, accuracy/projectile behavior where statically knowable, and each side's usable weapon range. Apply a continuous relative-range multiplier: for example, 21% more usable range multiplies that side's threat by about 1.21, while the reverse matchup receives the reciprocal advantage, rather than using a binary threshold. Normalize results into a documented exchange unit so a fixed threat value always corresponds to a predictable required number/value of Orcas, Apaches, or another queried unit type. Sum pairwise threat values for multiple defenders, then convert the total through the same exchange ratio. Compute immutable rules-level baselines once and cache them for O(1) lookup, but always incorporate actual veterancy changes to range, damage, reload, accuracy, HP/armor, or other relevant enabled traits. Choose one verified deterministic approach: lazily cache each effective veteran matchup after first encounter, bypass the baseline cache for veteran actors, or apply a simple level factor only if exhaustive comparison proves that factor reproduces the engine's full trait-modified calculation. In every case, cached/factored veteran results must match an uncached full calculation for the same live effective stats, including integer rounding; add cross-check diagnostics/tests that fail on divergence. Apply other genuinely live modifiers such as current HP/ammo and disabled weapons at lookup time when required. Handle actors with no valid weapon, multiple armaments, support-only roles, transformations/upgrades, missing armor/health, and symmetric/asymmetric matchups. Add focused unit tests with hand-calculated fixtures, every veterancy level/effect combination, cache/full-calculation equivalence, and cache-construction/performance checks. |
| CNC-69 | pending | **Use matchup threat—not economic cost—for air target safety and sheep counterplay.** Replace the current AA danger formula (`economic value × AA effectiveness/config override`) with CNC-68's aircraft-specific matchup exchange rating. A cheap actor such as a sheep must not look safe merely because its purchase cost is low: its threat to Orcas and Apaches depends on its weapons versus their armor/HP/range, while their ability and time/ammo required to kill it depends on their weapons versus its armor/HP/range. Preserve authored overrides only as explicit final tuning/disable controls, preserve true/veterancy range and live local safety checks, and sum all covering actors before converting the result into required Orca/Apache strength. Calibrate and log reference conversions so the established SAM benchmark remains intelligible (for example, a SAM threat requiring roughly five Orcas or seven Apaches must map consistently, while additional threats add proportionally). Revalidate undefended-first selection, AA clearing, sneak attacks, routing, retreat, repair, and transport safety so the new scale does not regress working air behavior. Integration gate: combine CNC-67's adaptive normal Skynet with the five-minute sheep-specialist cheat AI; Skynet must adapt its production, avoid wasting air power on misleading sheep economics, exploit safe paths around sheep to valuable harvesters, and decisively win complete seeded real matches. Include native non-sheep controls and report matchup-table values, selected targets/routes, composition over time, outcome, and performance. |
| CNC-70 | pending | **Prefer stealth harvesters exclusively when available.** When an AI's current technology, ownership, and prerequisites allow it to produce a stealth harvester, suppress ordinary harvester production and satisfy every new/replacement harvester request with the stealth variant. Derive this from buildability and harvester capabilities rather than faction-name or map-name checks, and switch back immediately if stealth harvesters become unavailable after a technology/prerequisite/ownership change. Count live, queued, reserved, captured, and transformed harvesters consistently so the normal harvester targets and CNC-55 emergency recovery do not double-order or stall. Do not replace or destroy existing ordinary harvesters merely to homogenize the fleet. Test openings, replacement losses, emergency recovery, technology switching, captured production, temporary prerequisite loss, queue contention, and native AIs both with and without stealth-harvester access in complete real matches. |
| CNC-71 | pending | **Threat-aware MCV expansion-site selection.** When an AI acquires an additional mobile construction vehicle, move it toward a useful deployment area at least 40 cells from every existing friendly building whenever the map contains a reachable legal candidate satisfying that separation. Replace random destination selection with a bounded deterministic coarse-map scan that scores reachable candidate cells by minimum friendly-building distance, live/general strategic threat-map danger along the route and at the destination, local construction/deployment legality, usable expansion space, and economic/strategic utility. Route away from or around enemy forces through the least-dangerous viable corridor, re-evaluate when threats, blockers, ownership, or deployment legality change, and choose the best deterministic fallback if no candidate can meet 40 cells. Reserve distinct expansion areas for simultaneous MCVs so they do not cluster or chase the same destination. Do not move the only deployed construction capability, strand MCVs on inaccessible terrain, wander indefinitely, or deploy beside an existing base merely because a random point was chosen. Test connected and island maps, dense existing bases, moving enemy armies, blocked/invalid destinations, several new MCVs, captured MCVs, no exact 40-cell option, route-threat changes, and full real matches with logging of candidate scores, chosen route/destination, recalculations, and deployment outcome. |
| CNC-72 | pending | **Fix macOS display scaling and input-coordinate drift.** Reproduce or diagnose the reported macOS graphical offset where the command bar extends below the screen boundary, selection boxes render slightly above their units, and targeting requires clicking below the displayed actor. Treat these as one likely logical-versus-drawable-size/HiDPI coordinate-transform defect until evidence separates them. Inspect the macOS/SDL window drawable size, Retina backing scale, UI scale, viewport transforms, cursor-to-world projection, window resize/fullscreen changes, and safe-area handling. Search the official OpenRA repository history and relevant-file commit history—not only its current code—for macOS, SDL, Retina, HiDPI, drawable-size, viewport, and input-coordinate fixes; backport the smallest compatible upstream solution where applicable. Apply one consistent scale/offset transform at the correct platform boundary rather than compensating widgets, selection boxes, and targeting independently. Preserve Windows/Linux coordinates, deterministic game state, screenshots, fullscreen/windowed transitions, and user-configured UI scaling. Because local hardware is Windows, add focused transform tests and diagnostics for logical size, drawable size, scale factor, mouse position, viewport origin, and projected world cell; use available macOS CI or require a clearly documented real-Mac verification matrix before marking complete. Test Retina and non-Retina displays, multiple resolutions/scales, windowed/fullscreen, monitor moves, resize, command bar bounds, actor selection boxes, targeting clicks, map edges, and spectator/gameplay modes. |
| CNC-73 | pending | **Audit repair-area and stealth-generator coverage performance using the resonator work as a reference.** Profile the user's repair-area and stealth-generator implementations on large maps and high-unit-count real matches, especially actor scans, range-cell enumeration, enable/disable or power transitions, ownership changes, creation/removal, and per-tick work. Replace polling or repeated full coverage calculations with deterministic event-driven invalidation or bounded spatial caching where evidence shows a real cost, while preserving exact repair eligibility/rates, passive allied repair behavior, stealth/cloak and detector interactions, power-state behavior, overlap handling, veterancy/conditions, and replay determinism. Ensure cached coverage cannot become stale when actors move, change owner, gain/lose power, are created/destroyed, or enter/leave the world; avoid duplicate repair ticks, cloak transitions, or permanent missed updates. Compare before/after CPU traces and identical-seed outcomes, then run at least one full large real match plus targeted power/overlap/movement edge cases. |
| CNC-74 | pending | **Chemical-tank mutant avoidance.** Chemical tanks must recognize hostile mutants created by their own attacks as immediate local threats and withdraw before those mutants can surround or kill them. Use live ownership/relationship and weapon-threat data rather than actor-name-only exceptions; choose a deterministic reachable escape cell and re-evaluate while mutants pursue, without abandoning a still-safe ranged engagement or fleeing through worse threats. Preserve chemical splash/mutation behavior, stealth behavior, ordinary squad ownership, and replay determinism. Test one and several newly created mutants, mutants created by allied chemical units, ownership changes, blocked escape routes, nearby higher threats, squad reassignment, and complete real matches. |
| CNC-75 | pending | **Iron Reaper covert-behavior parity.** Whenever Iron Reaper currently uses the covert technology branch, enable the compatible behaviors already implemented for native VIKI: covert production/config parity, stealth harvesters and generators, stealth-tank and chemical-tank harassment squads, red-Tiberium bomb missions, APC/transport specialist attacks, nuclear use, and any other capability-based covert behavior available by then. Reuse shared behavior modules and branch/capability configuration instead of copying VIKI code or keying solely on AI names. Activate and retire behaviors cleanly when Iron Reaper switches into or out of covert technology, preserve reservations and existing units, and do not pre-empt CNC-26C's final branch-specific strategic tuning. Benchmark branch-locked Iron Reaper against equivalent VIKI setups and test mid-game switches, lost/captured prerequisites, all-technologies mode, save/load, and complete real matches. |
| CNC-76 | pending | **Hold the first defensive tower for reactive placement.** Complete production of the opening's first defensive tower but keep it unplaced and ready during the first four game minutes. If an enemy enters the AI's base during that window, immediately place the held tower at the best legal reachable construction cell between the intruder/approach vector and the most valuable threatened base targets; score targets and candidate cells deterministically and fall back to the closest useful legal cell rather than stalling. At four minutes, if no intrusion triggered placement, release the tower to the existing normal first-tower placement behavior. Holding the ready tower must not cancel it, spend it twice, block unrelated parallel production queues, suppress emergency power/economy recovery, or survive invalidly across prerequisite/ownership loss. Coordinate with the MCV wall and first-economy-tower tasks. Test several attack directions, multiple intruders, blocked intended cells, destroyed/captured facts, save/load across the timer, exact timer boundary, no-attack control, and complete real matches. |
| CNC-77 | pending | **Optimized VIKI, Brutalis, and Iron Reaper openings.** Extend the proven configurable parallel opening system to VIKI, Brutalis, and Iron Reaper with capability/technology-aware alternatives rather than one copied faction list. Preserve concurrent independent queues and ordinary scaling whenever an opening goal is not using a queue. During the first five game minutes, VIKI and every other AI using this opening system must build power only when a required prerequisite needs it or the next wanted construction would otherwise enter low power; do not pre-build surplus power while harvesters, refinery/vehicle-production scaling, or their mandatory prerequisites remain affordable and viable. The power guard must re-evaluate live consumption, queued/held construction, losses, captures, and emergency recovery so it neither stalls a required building nor repeatedly queues redundant plants. VIKI should establish radar at approximately the same opening stage as Skynet, after its core minimum-power, economy, and production path; Brutalis and Iron Reaper may defer radar until normal post-opening construction so radar unavailability or expense cannot hold their openings incomplete. Give each AI a tested optimized order for early economy, defenders, infantry, production, harvesters, expansion MCV, power, and optional unavailable structures while retaining its intended branch identity and emergency-economy overrides. Do not make one missing prerequisite stall all construction, duplicate opening unit requests, suppress normal behavior on idle queues, or alter Skynet's already-working order. Compare each updated AI against its pre-task control in seeded full matches and require materially faster stable harvester/vehicle scaling without a weaker rush defense; test GDI/Nod starts, technology switching/all-technologies Iron Reaper, scarce cash, destroyed/captured power, queued next-building power thresholds, unavailable silo/tower/radar options, save/load, and complete real matches. |
| CNC-78 | pending | **Verify and enforce useful VIKI APC transport.** Instrument and test whether VIKI actually recognizes, fills, routes, unloads, and hands off owned or newly built APCs for useful infantry transport. Fix the smallest true owner if it currently walks eligible long-distance infantry instead. Coordinate with CNC-65 universal opportunistic transport and CNC-65A full/specialist-safe composition rather than duplicating those systems: VIKI must use granted and produced APCs whenever a compatible useful mission exists regardless of whether its opening selected deliberate APC-rush production. Preserve local defenders, reservations, safe early-contact unloading, capture/C4 intent, ordinary post-unload squads, and deterministic behavior. Test one APC plus ten granted infantry, normal production, several passenger mixes, nearby versus distant targets, both opening-strategy outcomes, blocked/lost APCs, and complete real matches with logs proving transport rather than walking. |
| CNC-79 | pending | **VIKI early APC-or-stealth opening choice.** At game start, make one deterministic configured strategy choice. One branch pursues the earliest viable APC-rush production/opening route. When that branch is not selected, pursue the earliest viable stealth-tank route, approximately power plant → barracks/Hand → refinery → radar → airfield, plus only mandatory power/prerequisite/economy recovery inserted as needed. VIKI's ordinary non-specialist opening baseline should otherwise remain comparable to the proven Skynet order. The alternatives must be mutually exclusive as opening emphasis, must hand unused queues back to normal parallel scaling, and must not prevent opportunistic APC use under CNC-78 or later stealth/transport behavior. Test both forced choices, multiple seeds, unavailable/captured prerequisites, low cash, early losses, actual time to first loaded APC or stealth tank, transition to normal scaling, and complete real matches against feature-disabled VIKI controls. |
| CNC-80 | pending | **Value-aware stealth-generator placement.** Replace random stealth-generator placement with deterministic scoring that first covers the highest combined economic/strategic value of owned eligible buildings not already cloaked, while considering legal placement, build radius, power, threat, and generator coverage. Score marginal newly protected value highest; allow limited overlap when it protects especially valuable or dense targets, but penalize coverage already supplied by other active generators so the AI does not waste most of a field. Re-evaluate when generators/buildings are created, destroyed, captured, lose power, or placement becomes blocked, without moving existing structures or scanning every cell every tick. Preserve stealth radius, power behavior, build limits, native VIKI and covert Iron Reaper compatibility, and deterministic replay state. Test facts, refineries, production, technology, power, defenses, sparse/dense bases, partial overlap, blocked ideal cells, power loss, expansion bases, and complete real matches against random-placement controls. |
| CNC-81 | pending | **Restore stealth-tank harassment behavior and threat discipline.** Reproduce the report that completed CNC-27 stealth-tank harassment is not operating reliably in current cumulative ordinary games; squads may form yet still fail because their threat avoidance and target progression are poor. Add bounded diagnostics covering eligibility, half-force allocation, generic reservations, squad creation, target candidates/rejections, detector/retaliation safety, routing, issued attack/crush orders, target completion/retarget, veterancy/killed value, and release after death/capture/transport/technology changes. Fix the smallest actual regression or configuration gate rather than merely forcing production. Reuse the excellent air AI's proven structure where applicable: search strategically, prefer undefended targets first, then deliberately clear the least-dangerous obstruction when no safe value is available; safely crush isolated infantry without firing or revealing cloak where that improves access. Never copy aircraft-only ammo/repair/AA assumptions into ground stealth logic. VIKI and covert-branch Iron Reaper must give formation of the first viable harassment squad priority over assigning early stealth tanks to ordinary armies; once that opening squad is formed, retain the configured two harassment groups when enough tanks exist and leave roughly half of later available stealth tanks in ordinary armies. The squads must visibly attack exposed harvesters/advanced power or other allowed opportunities and avoid defended/detected retaliation zones. Acceptance requires more than squad/order logs: across relevant full games, specialist tanks must reliably survive long enough to gain veterancy and substantial killed value, with deaths limited mainly to genuinely unavoidable accidents or favorable exchanges. Test granted and naturally produced tanks, first-squad priority and two through late-game group sizes, safe crushing, weakest-defense clearing, no-target/fully defended controls, destroyed/captured targets, detector changes, save/load, interaction with transport/APC/red-Tiberium reservations, and complete real matches against a feature-disabled control. |
| CNC-82 | pending | **Use captured technology with its intended behaviors.** Every AI, including Brutalis, must recognize and use captured or otherwise owned off-branch production technology and specialist assets through the same applicable behaviors as their native users. Derive activation from live buildability, prerequisites, actor capabilities, and ownership rather than the AI's authored faction/branch name. Captured stealth technology must enable appropriate stealth-unit production demand, stealth harvesters/generators, value-aware generator placement, stealth-tank and chemical harassment, red-Tiberium missions, APC/transport specialist use, nuclear/support powers, and ordinary squad integration whenever the required capability actually exists; equivalent recon/economy captures must likewise expose their relevant behaviors. Preserve native priorities when no captured capability exists, avoid copying whole foreign AI personalities, release/disable behavior cleanly after capture loss, and prevent multiple native/off-branch managers from issuing competing orders. Test each AI capturing factories, tech structures, individual stealth units/harvesters/generators, partial prerequisite chains, losing/recapturing assets, all-technologies Iron Reaper, save/load, and complete real matches against controls that own no captured technology. |
| CNC-83 | pending | **Credit attacking stealth harvesters as combat assets.** When a stealth harvester is explicitly reserved or assigned to an attack mission such as a red-Tiberium bomb run, count the economic value it destroys toward adaptive combat-production learning and allow adaptive combat production to request additional mission-capable stealth harvesters from the combat production budget/percentage. Ordinary harvesting and replacement demand must remain owned by the economy/harvester logic, and merely owning, travelling, harvesting, unloading, or defending with a stealth harvester must not create combat credit or duplicate production demand. Attribute kills only while the harvester has a valid combat mission, including direct weapon, impact, cargo explosion, and delayed-damage ownership where supported; avoid double credit with ordinary killed-value statistics or the red-Tiberium coordinator. Bound combat demand by live/queued/reserved mission capacity, available red Tiberium and viable targets so adaptive learning cannot starve the economy or spam idle harvesters. Preserve CNC-70's preference for the stealth variant whenever harvester production is appropriate, emergency-economy recovery, reservations, ownership changes, save/load, and deterministic replay state. Test successful and failed bomb missions, multiple victims from one explosion, mission cancellation, target/harvester capture, ordinary harvesting controls, empty cargo, no viable red Tiberium/targets, queue contention, adaptive ratio changes, and full real matches against a feature-disabled control. |
| CNC-84 | pending | **Repair-queue recovery for Skynet.** Reproduce the reported Skynet failure where a majority of a damaged force remains stalled instead of receiving repair orders, contributing to a loss against two VIKIs. Identify whether repair eligibility, queue ownership/capacity, reservations, pad selection, movement, stale orders, power/prerequisites, or completion handoff is the true owner; add bounded diagnostics that account for every damaged eligible unit and why it is repairing, queued, deferred, or excluded. Skynet must continuously dispatch eligible damaged units to available repair capacity, recover when pads/paths/units are destroyed, blocked, captured, powered down, or become available, and return repaired units promptly to their prior valid squad/mission. Do not overfill or thrash repair pads, send locally unsafe units into avoidable danger, steal units reserved for an incompatible active mission, or make healthy forces idle. Prove in the reported Skynet-versus-two-VIKI scenario and focused multi-unit/pad tests that a majority stall cannot recur; test damaged air and ground units, queue contention, partial repair capacity, path/landing blockage, power loss, destruction/capture, squad handoff, save/load, and complete real matches. |
| CNC-85 | pending | **Air-squad Stealth Tank hunt priority.** Set the Stealth Tank's configured priority to 3000 in the air-squad AI target scorer, solely as a hunt/target-selection priority: when that squad selects a target to hunt, a visible and otherwise eligible Stealth Tank must receive score/priority 3000 and be selected appropriately. This is not a build priority or production-composition rule; do not invent, change, or infer any production build priority, cross-technology production capture, queue arbitration, or production demand. No AI may capture or produce another technology branch's production capability. The separate correct rule is that, where the game's husk rules permit it, an eligible destroyed husk may be restored into its live unit type even when that live unit belongs to another technology branch; restoration does not grant foreign production capability. Run a full-engine adversarial comparison with feature-enabled and matched control evidence that records target selection and proves the configured 3000 priority changes eligible visible Stealth Tank hunting as intended, including ineligible/hidden/no-Stealth-Tank controls and ordinary target competition. Preserve visibility and eligibility rules, deterministic replay, all balance (frozen), production composition, and unrelated AI behavior. |
| CNC-86 | pending — balance frozen | **Revise Tiberium-detonation options and loaded-harvester damage behavior.** Re-spec and regress CNC-33A.2/CNC-33B without rewriting their completed evidence. Rename the lobby option “No unstable harvesters” to the positive “Unstable tiberium”: when selected, retain the existing unstable red-Tiberium behavior, including spontaneous/unstable loose-red-Tiberium explosions and spontaneous/unstable explosions of harvesters carrying red Tiberium; when deselected, neither loose red Tiberium nor a red-Tiberium-carrying harvester may spontaneously/unstably explode. Rename “No damaged Tiberium explosions” to the positive “tiberium explosions”: when selected, retain the existing shot-triggered loose blue/red-Tiberium explosion behavior; when deselected, loose Tiberium of either color must not explode merely because it is shot. These gates apply only to the specified loose-Tiberium spontaneous/shot events, not to player-caused harvester-cargo explosions: regardless of either setting, a harvester carrying blue or red Tiberium that is killed by damage or deliberately detonated must always explode using its existing color-specific reaction details. The blue loaded-harvester shot-burst regression remains CNC-86A; preserve its requirements and option interaction there without suppressing killed/detonated harvester explosions here. Preserve ordinary unaffected field/cargo behavior, deterministic replay/RNG discipline, mutation semantics, damage ownership, death/cargo handling, all unaffected lobby settings, and balance (frozen); do not invent unrelated AI behavior. It is wrong to invert either positive option, conflate loose-Tiberium events with harvester-cargo death/detonation, suppress player-caused killed/detonated red or blue harvester explosions, or change the existing harvester-specific reactions. Test the complete four-setting matrix; loose red spontaneous instability; loose red/blue shots; red/blue cargo harvesters under spontaneous-instability controls versus damage-killed and deliberate-detonation paths; the CNC-86A blue burst interaction; simultaneous deaths; save/load mid-sequence; AI and human sources; and complete real matches. The predicted change is clearly named positive lobby choices that independently gate only unstable red-Tiberium and shot-triggered loose-Tiberium explosions, while player-caused loaded-harvester cargo explosions always remain intact. This is needed so players can choose those loose/unstable effects without unintentionally removing established harvester-cargo combat reactions. |
| CNC-86A | pending — split from CNC-86; balance frozen | **Blue-Tiberium loaded-harvester shot-burst regression.** What happened: the required blue-specific damaged/shot loaded-harvester burst behavior is bundled with CNC-86’s option revision instead of being independently ready and regressible. This is wrong because a shot harvester carrying blue Tiberium must have its own reliable, player-visible damage outcome, rather than inheriting or being obscured by field-detonation or red-instability option behavior. When a loaded blue-Tiberium harvester is shot, it must create exactly four to seven staggered chemical/blue-Tiberium explosions at random valid cells within two cells of that harvester; a red-Tiberium harvester must not receive this blue-specific pattern. This behavior is in scope independently of both CNC-86 lobby options: enabling “No unstable harvesters” must only prevent spontaneous loaded-red instability, and enabling “No damaged Tiberium explosions” must only suppress damaged red/blue Tiberium detonation; neither option suppresses either color’s loaded-harvester shot behavior or this blue burst pattern. Preserve ordinary undamaged field/cargo behavior, normal loaded-red shot explosions, deterministic replay/RNG discipline, mutation semantics, damage ownership, death/cargo handling, and every unrelated lobby setting; do not alter balance (frozen). Test exact blue burst count, staggered delays, radius, random-valid-cell selection, invalid nearby cells/map edges, red-cargo control, the full CNC-86 option matrix, simultaneous deaths, save/load mid-sequence, AI and human damage sources, and complete real matches. The predicted change is that shot blue-Tiberium harvesters consistently create the distinct nearby staggered burst sequence regardless of the two option settings, while red harvesters and field-detonation rules remain correctly separated. This is needed to make the damaged loaded-harvester behavior observable, deterministic, and regression-safe without expanding CNC-86’s lobby-option contract. |
| CNC-88 | pending — high-priority engine reliability regression; balance frozen | **Bound and stream headless MAX benchmark collection.** `OpenRA.Game/Support/Benchmark.cs` currently retains every `BenchmarkPoint` in unbounded per-channel lists and writes CSV only at shutdown. `Game.cs` calls `benchmark.Tick(LocalTick)` outside/after `OrderManager.TryTick`, so outer-loop iterations are sampled even when no world tick advances. In two four-AI Archipelago `Launch.Benchmark` headless MAX runs this grew RSS to about 5.3 GB in 485.2 seconds and 6.2 GB in 577.6 seconds, requiring termination on an 8 GB/no-swap host; a completed 40,000-world-tick control wrote 1,362,671 rows in each of 12 CSVs (about 34 samples/world tick; 179 MB), while the killed runs' logs were only 1.6 MB and 4.5 MB. The second run used a lower maximum actor ID yet more memory, ruling out army-size runaway. Sample useful completed world ticks once rather than duplicate outer-loop iterations, and stream or use bounded buffers so benchmark output becomes available during the run and long games do not retain multi-gigabyte state. Preserve benchmark semantics needed by existing consumers, normal Benchmark-off behavior, deterministic simulation, headless MAX behavior, and all game balance (frozen); do not suppress useful telemetry merely to reduce memory. Add a lightweight headless MAX regression with many non-world outer loops proving exactly one useful benchmark sample per world tick, bounded RSS in the tens or low hundreds of MB rather than sustained unbounded growth, clean final CSVs, and matched Benchmark-off/on controls. The task is needed because instrumentation must not OOM worker/coordinator processes or delay all output until shutdown. Coordinate fixture setup/readiness with CNC-89, but keep benchmark engine ownership here. |
| CNC-89 | pending — high-priority fixture/readiness regression; balance frozen | **Validate focused Archipelago Lua fixtures and fail fast before endurance runs.** The intended CNC-39 fixture maps placed `World: LuaScript:` after `Rules: rules.yaml` in `map.yaml`; working fixtures place the World/LuaScript definition inside `rules.yaml`. Consequently no `lua.log` or fixture-ready marker existed, intended actor telemetry and termination did not run, and the worlds advanced to 80,000 and 105,000 ticks as strategic stalemates with repeated unreachable-route/transport scans. Correct focused fixture packaging at its true rules owner and make the headless/MAX harness fail during initial setup when the required ready marker or authoritative actor/build telemetry is absent, instead of consuming game slots for minutes and producing unusable evidence. Add one valid and one deliberately malformed fixture regression proving ready-marker success and clear fail-fast rejection, then run a bounded real Archipelago fixture game that emits the expected telemetry and reaches its intended bounded termination/conclusion. Preserve valid map loading, ordinary maps and headless/MAX simulation behavior, fixture isolation, and all game balance (frozen); do not fabricate readiness from the harness or weaken the marker/telemetry requirement. Coordinate the benchmark invocation with CNC-88, but keep fixture and readiness-validation ownership here. This is needed so rare-event custom maps provide causal proof rather than silently running a different stalemated game. |
| CNC-90 | pending — high-priority idle-unit and Commando recovery regression; balance frozen | **Recover genuinely unassigned controllable units and correct Commando demolition assignment/approach.** In several full-game and custom-map tests, AI-owned units became or remained idle after an assignment ended, became invalid, or was never created; a ready combat unit then remained wasted while a valid squad, mission, or nearby enemy existed. Periodically—but with bounded event-assisted scanning—detect genuinely unassigned/idle controllable units and adopt each into a role-appropriate existing or fallback squad/mission. A defensive fallback must perform sensible bounded work, including engaging a nearby valid enemy when appropriate, rather than leaving a combat unit permanently inert. Preserve legitimate non-idle ownership and states: repair/rearm/unload, transport cargo, explicit staging, capture/specialist reservations, retreat, production exit, and any other module-owned mission. Do not make economy units or noncombat specialists indiscriminate attackers, let modules fight over a unit, create scan/order spam, or introduce a large per-tick CPU cost. Specifically reproduce the many-Engineer/many-Commando custom-map case: capture conflicts were correctly resolved and capture should remain preferred over demolition when an Engineer can sensibly take the building, but some Commandos stood idle beside valid detonation targets while substantially farther Commandos received blind move/detonate orders, crossed hostile fire, and died. Select a nearby eligible unreserved Commando before a substantially farther candidate; ensure every eligible idle Commando receives useful reassignment; retain deterministic shared capture/detonation reservations and revalidate target ownership/relationship; and use a combat-/threat-aware bounded demolition approach that can fight through, react to, route around, hold, or withdraw from active danger rather than blindly walking across the map. An adjacent idle Commando ignored for a distant one, duplicate specialist ownership, demolition of a building that should be captured, hostile-fire crossings without reaction, or unrelated squads repeatedly cancelling specialist orders are failures. Coordinate with in-progress CNC-39/CNC-39A without changing their active contracts, and with CNC-50 late-game Engineer recovery, CNC-59 specialist transport/capture, CNC-53/CNC-51 transport ownership/safety, CNC-35 general squads, and CNC-84 repair handoff; this task is the newly specced regression follow-up. Required regression: recreate the many-Engineer/many-Commando map with near and far demolition candidates and active route pressure; assert nearest-eligible assignment, no duplicate ownership, capture-over-demolition, useful reassignment of released/idle Commandos, and a bounded safe/fighting approach. Follow with ordinary full-AI MAX games and old-behavior controls to detect ownership, performance, strategic, transport, repair, and squad-handoff regressions. The expected result is fewer permanently idle controllable combat units, stronger useful squads/local defense, and Commandos that act promptly and survive approaches more often, with balance frozen. |
| CNC-91 | pending — lower priority than primary defense clusters; balance frozen | **Skynet sparse advanced-guard-tower deterrence.** When Skynet has suitable existing advanced guard towers available, place them in a sparse, broadly distributed pattern around the map and around viable Tiberium fields/approaches so their existing stealth detection and long-range fire deter small stealth-harassment squads, protect resource access, and make it harder for covert opponents to re-establish stealth bases in those regions. This is a secondary map-control action: always keep it lower priority than Skynet's main defense clusters, including their walls and multiple tower types, and defer/cancel sparse placements when primary cluster, economy, prerequisite, power, or normal emergency recovery work needs the construction budget or queue. Coverage need not be complete and placement should prefer spread over dense local overlap; use legal, reachable, build-radius-valid positions without blocking harvesting, movement, refinery access, expansion, or the primary cluster layout. Re-evaluate field/area usefulness, ownership, buildability, destroyed towers, and changing build radius with bounded work rather than repeated whole-map scans or placement/order spam. It is wrong to turn this into a continuous wall of towers, build a dense ring around every field, prioritize it over direct-attack defenses, reserve construction indefinitely while an area is unsafe/unreachable, change tower statistics or stealth/balance values, or treat the towers as the primary response to a direct assault. Test sparse map/field spread, field access, stealth detection and firing at harassing units from the tower's existing range, no-harasser/low-resource controls, construction and queue contention with primary clusters, destroyed/captured/invalid/unreachable areas, changing Tiberium/build radius, save/load, and complete Skynet games against covert opponents versus a feature-disabled control. The predicted change is scattered existing advanced guard towers that create imperfect but useful anti-stealth deterrence and resource/map-control coverage without displacing core base defense. This is needed to reduce covert harassment and stealth-base rebuilding around Skynet's economic territory while preserving frozen balance. |
| CNC-92 | pending — VIKI strategic stealth-base distribution; balance frozen | **VIKI distributed stealth bases with concentrated, recoverable production.** VIKI must use stealth MCVs to tend toward multiple legal, geographically separated bases rather than concentrating every structure around one location. This is a strategic placement tendency, not a rigid partition: bases may overlap, cluster, or merge naturally as construction, terrain, threats, and recovery require. Some selected remote or distinct base areas must tend to be passive economic/stealth bases: a Construction Yard (`fact`), power, refineries, and Temple of Nod as appropriate, protected by stealth generation, but with no infantry or vehicle production structures and no resulting infantry/vehicle production or attack traffic that would reveal the base. Keep infantry/vehicle production deliberately concentrated in one designated production-base cluster, which must be difficult to attack due to its concentrated support and defenses. If that production cluster is knocked out or loses the required production capability, immediately pause infantry and vehicle production everywhere while a new suitable production-base cluster is established; only resume it after the replacement cluster has four eligible live, owned, powered, buildable infantry/vehicle production structures. Re-evaluate location ownership, stealth coverage/power, construction radius, path/build validity, destroyed/captured structures and MCVs, and production-cluster loss/recovery with bounded, event-assisted work and deterministic state; do not continuously rebalance base membership merely to preserve an ideal layout. Do not turn every expansion into production, let a selected passive base queue or produce infantry/vehicles, unnecessarily spread active production among remote bases, resume after only one replacement factory, designate an arbitrary group of four distant structures as a cluster, permanently stall production after recovery, require exact minimum distances, exclusive territorial ownership, perfect base classification, or expensive continuous rebalancing, or change unit/building statistics, costs, stealth mechanics, reveal rules, resource growth, or any other balance value. Test detection/reveal behavior in real matches so passive bases remain unobserved until existing legitimate detection/reveal conditions occur; demonstrate the intended broad geographic distribution where the map and game state permit while allowing bases to overlap, cluster, and merge; verify selected passive economic bases contain the intended fact/power/refinery/Temple-of-Nod role and do not produce infantry or vehicles while classified passive; verify one spatially concentrated production cluster owns active infantry/vehicle production; destroy/capture/power down that cluster and prove the global production pause; then establish a replacement cluster and prove production resumes only when four eligible production structures are live in that cluster. Include no-stealth/power-loss, invalid/unreachable expansion, MCV/structure loss and recapture, natural base merging, queue contention, save/load, deterministic replay, and complete VIKI games against a feature-disabled control. The predicted change is a harder-to-eliminate VIKI that generally retains quiet, distributed economic bases while exposing production and its associated reveal risk in one recoverable location, without forcing artificial base separation. This is needed because passive stealth bases can survive and finance recovery after an exposed production base is found or destroyed, without changing balance. |
| CNC-93 | pending — high-priority orchestration reliability bug/polish; balance frozen; eligible for a future coordinated round | **Enforce Policy Reviewer scratchpad replacement output and atomic promotion.** In the RC1 CNC-42 review response, the Terra policy process exited 0 and wrote a usable `POLICY-REVIEW.md` verdict but omitted `POLICY-SCRATCHPAD.md`; promotion therefore failed closed and persistent learning was lost. This is wrong because every successful Policy Reviewer role must supply both its verdict and a complete bounded replacement for shared cross-cycle policy knowledge, and a seemingly successful role must not silently discard that learning. Make the launcher/supervisor role-output contract explicit: a successful Policy Reviewer must emit UTF-8 `POLICY-REVIEW.md` and a complete UTF-8 `POLICY-SCRATCHPAD.md` replacement of at most 3,000 Unicode characters. Validate both outputs before success/promotion; missing, unreadable, invalid-UTF-8, over-limit, or otherwise invalid scratchpad replacement must fail and clearly report the role-output error, preserve the canonical scratchpad atomically and unchanged, and permit a safe retry. A valid reviewer response must atomically replace/promote the canonical bounded scratchpad and always participate in persistent policy memory. Add focused launcher/supervisor regressions for valid promotion, omission despite exit 0/useful verdict, malformed or over-limit replacement, and retry after failure, proving diagnostics, non-destructive failure, and atomic canonical preservation/replacement. Scope is orchestration/role contract only: do not change game balance, reviewer policy content, or active task contracts. The predicted change is that missing or invalid replacements become visible retriable role failures, while valid reviews reliably retain bounded cross-cycle expertise. |
| CNC-94 | pending — high-priority coordination-infrastructure bug/polish; balance frozen; eligible for a future coordinated round | **Make coordinated resource-slot diagnostics truthful and resource namespaces globally capacity-safe.** Verified implementation evidence: `scripts/with_resource_slots.py` uses advisory kernel `fcntl.flock` on `<resource>-<index>.lock` files. JSON diagnostics, including holder/guardian PID text, are not ownership authority, and lock files are intentionally not deleted on normal completion. The kernel releases the flock automatically when its file descriptor/process exits, so ownerless files or stale JSON are cosmetic metadata and do not themselves block a slot; prior quarantine of such files was not evidence of a product lock-recovery failure. The real risks are (1) stale diagnostics misleading a human or coordinator about slot status and (2) inconsistent lock-directory namespaces, such as round-local versus shared directories for the same capacity, defeating intended global accounting and allowing unintended concurrent games/builds. Audit every resource-slot caller and define/unify the intended namespace ownership so each globally limited resource has one consistent capacity domain, while explicitly documenting retained diagnostic lock paths or safely cleaning only metadata that is proven not to disturb a live flock. Never unlink, replace, or recreate a live lock path based on JSON, PID, guardian text, or inferred staleness; actual kernel flock acquisition is authoritative. Preserve advisory-lock atomicity, capacity and exclusivity, policy-scratchpad serialization, diagnostics, compatible safe invocation, isolation, and all game balance (frozen). Add focused regressions that inspect actual flock ownership and contention; prove release after normal completion and process death despite retained stale files/metadata; prove concurrent acquisition remains bounded by capacity; prove diagnostic cleanup/documentation cannot interfere with a live flock; and verify every game/build/policy caller uses the intended consistent namespace, including controls demonstrating that split namespaces would otherwise violate global capacity. The expected result is clear truthful status information and one auditable, globally enforced capacity domain per shared resource, without treating cosmetic stale metadata as a live lock failure. |
| CNC-95 | pending — high-priority coordination-infrastructure bug/polish; balance frozen; eligible for a future coordinated round | **Detect and safely recover interrupted external coordinated workers.** During active coordinated round `20260807-bug-polish-03`, all five external worker Codex processes and their launcher-supervisor processes terminated around 16:48–16:49 UTC, together with their game/build children, while every `process.json` remained falsely marked `running`. The VM had not rebooted (booted 2026-08-04), no tmux server survived, and the five task worktrees retained uncommitted changes. This is wrong because unattended development silently stopped across every workstream and required manual process inspection before work could resume. Where feasible, make long-running external workers independent of ordinary coordinator parent/client/session loss and Codex client updates; additionally provide a bounded watchdog/recovery path that validates stable process identity and tree liveness, detects when both worker and supervisor are dead, atomically marks stale/interrupted status instead of reporting `running`, coordinates automatic dead-lock quarantine/reclamation with CNC-94, and relaunches the exact assignment from its durable worker state. Recovery must preserve and continue any dirty worktree and existing evidence without reset, cleanup, overwrite, duplicate worker launch, duplicate cycle accounting, or promotion of a build/game/test that was only partially completed when interruption occurred. Preserve explicit stop/cancel behavior, live-worker exclusivity, role/model/sandbox/path-envelope enforcement, task-branch isolation, monitoring diagnostics, and all game balance (frozen); do not treat an unverified PID as the original worker, accept stale status as healthy, reconstruct an assignment from coordinator memory, or require manual inspection for routine recovery. Add focused regressions for parent/client disconnect or update, simultaneous worker-and-supervisor death, partial process-tree death, stale `process.json`, PID reuse, dirty-worktree relaunch, interrupted game/build evidence, duplicate-recovery races, explicit stop, and a multi-worker interruption, proving that live work is not duplicated, partial tests are not accepted, cycle counts remain exact, and the original assignments safely resume. The predicted change is that feasible worker sessions survive ordinary control-session disruption and every genuine interruption is detected promptly and resumed from durable state without losing or overwriting work. |
| CNC-96 | pending — user-reported periodic-stall diagnosis/regression; balance frozen | **Diagnose and remove periodic gameplay stalls on very old hardware without masking them as low average performance.** A user reports intermittent/periodic game freezes while running LibertyDawn on a very old computer. Related user follow-up (2026-08-07): "Economy guard squads seem to work, but I worry one of the new code changes may have spiked CPU usage. I can test on a better PC tomorrow." Clarification (2026-08-07): the user does not blame Economy guard squads; retain their apparent functional correctness only as an unrelated positive behavior observation and keep causal investigation component-agnostic until evidence isolates a cause. Related active feature: CNC-42 Economy field defense. Additional conditional solution direction (2026-08-07): only if profiling attributes meaningful stall cost specifically to per-Harvester guard planning, avoid one expensive independent planner per Harvester. Prefer guards associated with nearby Tiberium fields/areas or shared local groups, using simple fuzzy rules, bounded low-frequency local enemy checks, and ordinary move/guard/attack orders when enemies are detected; do not solve graph theory. This must preserve protection of exposed Harvesters, avoid cross-map churn and duplicate ownership, and freeze balance without unit-cap reductions. Phase staggering reduces coincident spikes but not total planner work; separately compare both it and actually reducing total guard-planning work against the current/control full-engine behavior before adopting either. Additional user hypothesis (2026-08-07): "Or perhaps large debug reports are being written to disk in playtest releases also. Not sure." Treat possible large debug/report disk-I/O bursts in playtest releases as an unconfirmed alternative to CPU-heavy AI logic, not a conclusion. Another freeze theory could be that all the AI work we spaced out with 1 or 2 seconds end up on the same tick. In this case that tick would be overloaded. Preserve this as a plausible hypothesis, not a conclusion. Inspect per-tick duration and work attribution, and determine whether periodic AI modules with equal or harmonic intervals share a phase and coincide at repeatable ticks/LCM boundaries across several bots. Compare aligned scheduling with safely phase-staggered scheduling in a matched full-engine scenario, preserving every module's average cadence and strategic behavior. Average CPU hiding tick spikes, merely lengthening all intervals and weakening AI, nondeterministic jitter that makes tests unreproducible, staggering one bot while all others remain aligned, and changing balance or unit caps are failure indicators. If evidence supports a solution, prefer simple stable per-module/per-player phase offsets or bounded work scheduling; do not solve an elaborate scheduling problem prematurely. Compare the same map/seed/roster/build where possible on the better PC to distinguish a product spike from limitations of the very old computer. Useful future evidence includes machine specifications, freeze frequency/duration, per-process CPU/memory, simulation-tick progress, and correlations with AI work, debug/report I/O, memory/GC, simulation load, or host conditions. This is wrong because gameplay should progress smoothly enough under supported play and a repeating halt may indicate a bounded subsystem or engine spike rather than uniformly slow hardware. Establish a controlled, instrumented reproduction using the reporter's available machine details and workload where obtainable, plus representative constrained-hardware controls; record frame/present and completed simulation-tick duration distributions, stall cadence/duration, actor/load state, GC/allocation or memory pressure, AI work, and host scheduling/thermal limitations so sustained low FPS is distinguished from an actual periodic pause. Compare debug/report-enabled versus disabled settings/builds while holding map, seed, and roster constant; measure log/report growth and write bursts alongside per-process CPU/memory and simulation-tick progress, and verify the playtest-release logging defaults. Exercise ordinary and adverse real-AI play, save/load, and increasing simulation load, and correlate each observed stall with the responsible subsystem or explicitly preserve the evidence when it cannot yet be assigned. Use CNC-47's baseline where applicable but do not alter its active contract. If a culprit is confirmed, make the smallest bounded behavior-preserving change that removes or bounds the spike; preserve deterministic simulation/replays, supported CNC scope, normal gameplay policy, and all balance (frozen). Average-only measurements, modern-hardware-only validation, reducing unit caps or changing balance to manufacture an improvement, treating sustained slow FPS as proof of a periodic stall, globally removing useful opt-in developer diagnostics, or claiming a fix without an instrumented low-end/full-engine scenario are failures. The expected result is either no periodic freezes in the controlled supported scenario or clear causal evidence separating AI, debug/report disk-I/O, memory/GC, simulation-load, and host-limit causes. This task is needed because a periodic halt materially harms play even when average throughput appears acceptable. |
| CNC-97 | pending — user-reported aircraft-husk Engineer lifecycle regression; balance frozen | **Verify and, if applicable, repair AI Engineer discovery and claim of new aircraft husks.** A user suspects that AI Engineers do not recognize newly added aircraft husks as capturable/restorable. This is wrong if an aircraft husk that is normally eligible for restoration/capture fails to participate in the same specialist target and claim lifecycle as other valid husks; if aircraft husks are intentionally not Engineer-restorable, that rule must instead be explicit and evidenced. Create a controlled full-engine scenario containing a normally eligible new aircraft husk and prove whether one Engineer discovers it, obtains the sole deterministic claim, approaches it through a reachable safe route (and compatible transport where required), and restores/captures it. Diagnose the true capability/eligibility, target-ranking, reservation, movement, transport, or restoration owner before changing behavior. If confirmed, recognize the actor's actual husk/capture capability rather than hard-coded actor names, while preserving ordinary vehicle/building capture, aircraft-specific rules, claim cardinality, reachability/value/safety ranking, ownership changes, save/load, and deterministic replay. A name-based special case, eligibility for a non-restorable husk, duplicate claims, blind cross-map moves, unit-balance changes, or a claimed fix supported only by unit tests rather than the full-engine scenario are failures. Coordinate with active CNC-44 aircraft-husk creation and CNC-50 Engineer recovery without changing either active contract. The expected result is explicit, correct aircraft-husk policy and, where eligible, one appropriately chosen Engineer that discovers, claims, approaches, and restores/captures the husk. This task is needed so new aircraft husks do not become silent unusable recovery opportunities. |
| CNC-26C | pending; pinned final and requires user design questions | **FINAL TASK — branch-adaptive Iron Reaper strategy and build priorities. Complete every other task first, including tasks added later.** Only after the rest of the task sheet is complete, go beyond first-pass config parity so Iron Reaper changes its strategy, production mix, build priorities, squad use, and timing according to its current technology branch. Before planning or coding this task, ask the user structured questions about the intended branch identities, transition behavior, priority inheritance versus Iron Reaper-specific overrides, and all-technologies mode. Then run controlled and real-match benchmarks: Iron Reaper against each relevant opposing AI, and branch-locked Iron Reaper against the native user of that branch and equivalent opponents. Record whether it gains a useful switching advantage while performing comparably to the native branch AI when locked to the same technology. Do not execute this task autonomously until every other task is complete and those questions are answered. |
