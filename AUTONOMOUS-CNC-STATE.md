# Autonomous CNC State

- Task sheet: `AUTONOMOUS-CNC-TASKS.md`
- Deferred work: `DEFERRED_WORK.md`
- Completed tasks: `CNC-19` (11 cycles, PR #38 green); `CNC-20` (12 cycles, PR #39 green); `CNC-21` (12 cycles, PR #40 green); `CNC-22` (8 cycles, PR #41 green); `CNC-23` (11 cycles, PR #42 green); `CNC-24` (14 cycles, PR #43 green); `CNC-24.5` (20 cycles, PR #45 green); `CNC-24.6` (14 cycles, PR #46 green); `CNC-25` (22 resumed cycles plus retained 28-cycle history, PR #44 green); `CNC-26` (12 cycles, PR #47 green); `CNC-26A` (11 cycles, PR #48 green); `CNC-66` (4 cycles, PR #51 green); `CNC-26B` (8 cycles, PR #53 green); `CNC-27` (15 cycles, PR #54 green)
- Active task: CNC-28 at cycle 0/30 on branch `agent/cnc28-stealth-chem`, cumulatively based on green CNC-27 PR #54 head. Draft PR creation follows this initial planning commit.
- Integration checkpoint: PR #49 (`agent/cnc-workflow-portability` -> `bleed`) was merged and contains all cumulative work through CNC-26A plus the portable skill and coordinator files. PR #50 keeps later task-sheet updates durable; CNC-66 PR #51 targets that queue branch.
- Next cumulative base: `origin/agent/cnc66-validator` (PR #51 head) for CNC-66A so both the repaired validator and unmerged durable task updates remain in the cumulative series.
- Publication: one task branch and pull request; never push directly to `bleed`
- Cumulative rule: CNC-20 and later use the preceding task PR branch as their base
- Aged reference: insane PR may be inspected for ideas/provenance only
- CNC-25 resume state: `complete` at 22/30 resumed evidence cycles. Concurrent landed pickup and explicit post-drop assault/abort handoff passed local acceptance, three clean adversarial engine tests, final regression, 272 unit tests, strict Debug build, interface checks, CNC YAML validation, and green Linux/Windows checks at commit `8bd9305ff1`. Preserve the original 28-cycle history and regression evidence directories.
- Resumed task: `CNC-24.6` at 14/30 cycles on branch `agent/cnc24-6-headless-max`, commits `5755575ab5` and `1b7398c288`, PR #46. Local acceptance, three clean adversarial cycles, final regression, 277 unit tests, strict build, interface checks, and CNC YAML validation pass. Identical Fastest controls were demonstrably nondeterministic: control A ended at tick 2,455, while control B remained active past tick 25,431 after ten minutes, so exact cross-run replay equality is not a valid gate. Headless save/load advanced beyond tick 46,000; the five-bot high-load run reached about 500 mobile units and ended naturally at tick 16,821.
- CNC-24.6 completion gate: PR #46 is clean and both required Linux and Windows checks passed at commit `1b7398c288`; task complete at 14/30 cycles.
- CNC-26 base: cumulative branch `origin/agent/cnc24-6-headless-max` (PR #46 head). Planned branch: `agent/cnc26-tech-switching`.
- CNC-26 literal acceptance: after an enemy changes branch, Iron Reaper alone observes the branch, waits the configured approximately two game minutes, and completes the first upgrade of the counter branch (covert counters economy, recon counters covert, economy counters recon). Other AI types do not switch, premature switching is forbidden, and captured/off-branch production remains usable.
- CNC-26 contention inventory: unit and base builders, production queues, opening build-order locks, adaptive cross-queue selection, emergency-economy recovery, owned/captured prerequisite providers, upgrade downgrade/`maxupgrades` rules, actor ownership changes, and enemy observation/defeat transitions.
- CNC-26 implementation: configurable `TechnologyCounterBotModule` is gated to Iron Reaper, uses deterministic game-tick observations and counter mappings, peels incompatible ordinary branches before buying the target branch, preserves branches under `Technology Level: Extra`, pauses for emergency economy/opening-refinery recovery, and emits bounded decision/block/completion logs. Pure policy tests cover ties, delay, mappings, downgrade selection, and tier ordering.
- CNC-26 cycle evidence: cycle 1 exposed an opening-policy fight over Recon and led to removing Iron Reaper's conflicting opening unlock; cycle 2 passed Economy to Recon; cycle 3 passed a live Economy to Covert enemy transition followed by an exact 3,000-tick delayed Covert to Recon counter; cycle 4 ran to natural game over, passed Recon to Economy decision timing and emergency-economy pause/recovery, but ended before Economy III completed. Evidence is under `AUTONOMOUS-CNC-LOGS/cnc26-cycle1-*` through `cnc26-cycle4-*`.
- CNC-26 later evidence: cycle 5 cleanly completed Recon-to-Economy III; cycle 6 saved mid-observation and resumed the retained delayed decision after load; cycle 7 preserved Economy III while adding Covert III under `Technology Level: Extra`; cycle 8 reset the delay when a multi-enemy dominant branch changed from Recon to Covert and did not apply the stale counter; cycle 9 exposed and fixed faction-start initialization, then proved a Nod Iron Reaper retained and completed its owned Covert branch without an early Economy detour.
- CNC-26 final local evidence: cycle 10 completed two successive multi-enemy replans through Recon III and Economy III after adding obsolete-request cancellation; cycle 11 proved VIKI and Skynet retain ordinary adaptive upgrade behavior with zero technology-counter activity; cycle 12 was a complete graphical Fastest real match at ordinary 20,000 credits, completing Economy-to-Covert III and a live Covert-to-Recon III switch before Iron Reaper eliminated VIKI's mobile force 300-to-0 and reached the result screen. The replay is archived under `AUTONOMOUS-CNC-LOGS/cnc26-cycle12-fastest-natural-result-20260804`.
- CNC-26 local gates: strict Debug build passed with zero warnings/errors; 285 unit tests and both trait-interface checks passed. `utility.cmd cnc --check-yaml` emitted no diagnostics but exceeded a 20-minute local timeout, so it is explicitly inconclusive and GitHub Linux/Windows checks remain the authoritative whole-rules gate.
- CNC-26 completion gate: PR #47 is clean and both required checks passed at commit `1720a9b398` (Linux 3m13s, Windows 5m38s); task complete at 12/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/47
- CNC-26A base: cumulative branch `origin/agent/cnc26-tech-switching` (PR #47 head). Literal acceptance: with `Technology Level: Extra`, Iron Reaper owns and can use Economy III, Recon III, and Covert III concurrently without downgrades; with ordinary technology level it retains CNC-26 exclusive counter switching. Skynet, VIKI, Brutalis, and legacy bots remain on their authored branch behavior under both settings.
- CNC-26A cycles 1-3: an Extra-mode GDI Iron Reaper completed Covert III and Recon III while retaining its starting Economy III; the ordinary-mode regression downgraded Economy and completed only Covert III; an Extra-mode VIKI-vs-Skynet control emitted zero technology-counter lines while Skynet retained its authored Recon upgrades and Economy downgrades. No fatal/unhandled game errors occurred.
- CNC-26A cycles 4-7: a Nod-start Iron Reaper retained Covert III, completed Economy III, and requested final Recon III; a tick-9000 save captured the live Recon-to-Covert transition; two direct reloads proved in-flight Covert I ownership and ultimately completed Covert III with all three branches at tier III. The complete save/load pass is the first clean adversarial cycle.
- CNC-26A cycle 8: in an Extra-mode three-bot match, enemy dominance changed from Economy to Recon while Iron Reaper's Recon III was in flight. It retained the useful request, updated its preferred counter, and completed all branches at tick 12127 without downgrade or fatal error. This is clean adversarial cycle two.
- CNC-26A cycle 9: a 5,000-credit stress match exposed the pre-existing opening-recovery failure (four structures, zero harvesters, eventual capture). The technology module remained paused throughout and consumed no recovery queue/cash, so this is a failed stress scenario rather than a CNC-26A regression or clean adversarial pass.
- CNC-26A cycle 10: at 10,000 credits, the module stayed paused while Iron Reaper completed its refinery, eight-structure opening, five harvesters, and expansion MCV. It then survived enemy branch churn and completed all three tier-III branches at tick 13877. This is clean adversarial cycle three.
- CNC-26A cycle 11: a complete graphical Fastest Extra-mode match ran to the result screen. Iron Reaper completed all branches at tick 12377, then reduced VIKI from 100 mobile units/13 harvesters to 0/0 while finishing at 181/15. Replay and logs are archived under `AUTONOMOUS-CNC-LOGS/cnc26a-cycle11-full-graphical-fastest-result-20260804`.
- CNC-26A local gates: strict Debug build passed with zero warnings/errors and all 286 unit tests passed. The explicit-interface utility emitted no diagnostics but exceeded five minutes; like the known exhaustive-validator issue now tracked by CNC-66, it remains inconclusive pending authoritative GitHub Linux/Windows checks.
- CNC-26A publication: commit `e5883edacb` is pushed on `agent/cnc26a-all-technologies`; draft PR #48 targets `agent/cnc26-tech-switching`, is mergeable, and has green Linux/Windows checks: https://github.com/Realpra1/LibertyDawn/pull/48
- CNC-26A completion gate: PR #48 is clean and both required checks passed at commit `e5883edacb` (Linux 3m06s, Windows 4m17s); task complete at 11/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/48
- Next eligible task: CNC-66, using the post-PR-#49 base described above. CNC-66A follows it, then CNC-26B. CNC-26C is permanently pinned last: complete every other current and future task first, then ask its required user-design questions before implementation.
- CNC-66 literal acceptance: the documented Windows `utility.cmd cnc --check-yaml` path must complete the full CNC validator set with useful stage/progress diagnostics in bounded time and the same failures/results as the direct utility path. It must not enter an interactive prompt loop, silently skip maps/lints, or change LibertyDawn gameplay/content.
- CNC-66 contention/coverage inventory: Windows batch wrapper and argument forwarding, build-on-demand, utility executable startup, mod discovery/loading, lint selection, rules/sequences/maps loading, map enumeration, explicit/conditional interface checks, missing-sprite checks, process exit/timeout handling, CI shell differences, and existing launcher/debug processes.
- CNC-66 initial evidence: CNC-24.5 recorded the interactive-wrapper mistake while the direct utility executable completed full validation in 19 seconds; CNC-26 later reproduced a silent 20-minute `utility.cmd` timeout and CNC-26A reproduced a silent five-minute interface-utility timeout. No validator process is currently running, and no dedicated CNC-66 raw log exists yet.
- CNC-66 result: the Windows wrapper, not YAML linting, caused the apparent hang by ignoring supplied arguments and entering the interactive prompt. Backported official OpenRA commit `07ec2d03fbfb` behavior with child exit-code propagation. Direct validation completed in 18.132 seconds; the repaired wrapper completed in 17.765 seconds with byte-identical output. Quoted invalid-map failure propagation and interactive compatibility passed.
- CNC-66 integrated evidence: a seeded two-Skynet versus three-Brutalis headless MAX Empire Earth match ended naturally at tick 27,880 with active bots and no new fatal/Lua/unhandled error. Strict build/interface checks, 286 unit tests, and pre-report Linux/Windows CI passed. Evidence is under `AUTONOMOUS-CNC-LOGS/CNC-66/`; report is `AUTONOMOUS-CNC-REPORTS/CNC-66.md`.
- CNC-66 publication: draft PR #51 targets `agent/cnc-task-sheet-updates`: https://github.com/Realpra1/LibertyDawn/pull/51
- CNC-66A result: Empire Lars was not crashing on Windows; its apparent startup freeze came from rebuilding all 146 resonator coverage regions for thousands of resource cells during `WorldLoaded`. Event-driven invalidation reduces measured graphical startup from 30.283 seconds to 4.772 seconds while preserving powered/unpowered transitions, 7.5x configured spread acceleration, red-Tiberium instability cancellation, damage explosions, and one live timed-update chain per resource cell. A full five-AI Empire Lars match ended naturally with 99 live modifier refreshes and no scheduler mismatch or fatal error; a seven-minute Empire Earth control kept all five bots active without a new fatal error. Old replays diverge at frame 51 because the old startup accidentally scheduled the first protected resource cell twice; same-build replay playback completes normally. Report: `AUTONOMOUS-CNC-REPORTS/CNC-66A.md`.
- CNC-66A publication: draft PR #52 targets `agent/cnc66-validator`: https://github.com/Realpra1/LibertyDawn/pull/52
- CNC-66A green gate: implementation commit `592f8ff3f1` passed Linux in 3m24s and Windows in 4m42s. Remaining gate: verify startup and logs on real Mac hardware. The next autonomous task is CNC-26B.
- CNC-26B literal acceptance: while Iron Reaper owns covert technology it must actually build stealth tanks, stealth harvesters, and stealth generators and launch available nuclear support powers. The implementation should reuse the exact relevant VIKI values/behavior without importing unrelated VIKI strategy, must preserve ordinary and Extra-mode technology switching, and must not alter native VIKI/Skynet/Brutalis configuration.
- CNC-26B contention inventory: `BaseBuilderBotModule@ironreaper` building fractions/limits and placement, `UnitBuilderBotModule@ironreaper` unit whitelist/limits, shared `SupportPowerBotModule@b`, Temple prerequisites, technology-counter owned upgrades/downgrades, Extra-mode simultaneous branches, captured/off-branch production, opening/emergency-economy queue gates, and native-bot config isolation.
- CNC-26B result: Iron Reaper now uses VIKI's exact covert first-pass values for stealth generators (`2%` building ceiling), stealth harvesters (`100`, limit `50`), and stealth tanks (`15`), and excludes stealth harvesters from combat squads like VIKI. Existing shared support-power targeting remains the behavior owner; an opt-in file diagnostic records charged support-power scans and selected targets without changing target scoring.
- CNC-26B engine evidence: natural Extra-mode games built all required assets; an equal-Covert-III focused real-AI fixture issued seven Iron Reaper nuclear target orders; an ordinary-tech full match built Covert III and all required assets before Iron Reaper eliminated Brutalis 180 mobile units to 0; and a full branch-churn match downgraded Covert, completed Economy III, built no further covert assets, and ended naturally without a queue stall. Three clean adversarial cycles passed; invalid harness cycles remain recorded in `AUTONOMOUS-CNC-REPORTS/CNC-26B.md`.
- CNC-26B completion gate: strict Debug build passed with zero warnings/errors; CNC YAML completed in 20.7 seconds; both trait-interface checks passed; 300/300 unit tests passed. Draft PR #53 targets the cumulative CNC-66A branch and is mergeable; Linux passed in 3m17s and Windows in 4m42s at commit `eed40a5e8b`.
- CNC-27 literal acceptance: once an eligible AI owns at least two stealth tanks, a deterministic specialist manager reserves roughly half for at most three small stealth squads and leaves the rest available to existing ground squads. Harassment squads prioritize exposed harvesters and advanced power, may grow late for rapid fact kills, crush safely isolated infantry, and may kite an isolated shorter-range tank. A cooperative attack squad prioritizes tanks. Specialist squads must not deliberately fire inside detector, defense, or retaliation coverage unless a bounded careful-clear rule explicitly judges the local exchange favorable. Dead, captured, transported, or no-longer-eligible tanks must be released without stalling either manager.
- CNC-27 forbidden outcomes: treating stealth tanks as aircraft, importing aircraft-only ammo/repair/AA assumptions, reserving every stealth tank, issuing competing ordinary/specialist orders, cheating on hidden enemy actors beyond the AI's existing knowledge policy, repeatedly scanning the full world per tank/tick, walking through known defense or detector zones for a juicy target, firing beside ordinary retaliators, losing deterministic save/replay behavior, or changing native air-squad behavior.
- CNC-27 contention inventory: `SquadManagerBotModule` active/base/squad ownership and save data; transport reservations/missions; `STNK` cloak, hold-fire stance, crush locomotor, dual armaments, range, target types, death/husk and ownership changes; `IVisibilityModifier`/detectors; enemy weapon retaliation/range; coarse strategic targeting and threat-aware route helpers; actor-value/priority configuration; captured/off-branch units; disabled bot traits; world/local random determinism; bounded scan/update intervals; and VIKI/Iron Reaper versus legacy bot configuration.
- CNC-27 initial design: add a separate configurable stealth-squad manager plus a generic unit-reservation seam consumed by ordinary squads. Reserve zero of one tank, both of the first pair, then approximately half as numbers grow; fill two two-tank harassment groups before a cooperative attack group. Keep target scoring/threat geometry in pure helpers suitable for focused tests, issue only bounded periodic orders, and retain ordinary squad ownership for all unreserved tanks.
- CNC-27 result: a dedicated manager reserves roughly half of eligible stealth tanks into at most two harassment groups and one tank-only attack group, while generic reservations prevent contention with ordinary ground squads. Periodic live weapon/detector geometry rejects guarded routes, allows isolated shorter-range tanks to be kited, applies late fact priority only to grown groups, and includes defenses regardless of creation order. Lifecycle rebalancing releases captured/dead/ineligible actors and recreates pairs from new production.
- CNC-27 cycle evidence: cycles 3, 6, 8, and final review found and fixed respectively ground-target classification, initial reservation contention, invented zero-range detectors, and late-threat truncation. Final post-fix cycles 13-15 passed guarded late-obelisk avoidance, isolated tank/economy behavior, and capture/death/reinforcement rebalancing. Cycles 13-14 ended naturally; cycle 15 was a focused regression after its exact expected transition sequence.
- CNC-27 local gates: strict Debug build passed with zero warnings/errors; 314 unit tests, both interface checks, and the complete CNC YAML/map validator pass. Report: `AUTONOMOUS-CNC-REPORTS/CNC-27.md`. Draft PR #54 targets `agent/cnc26b-branch-config`; implementation-head Linux and Windows checks passed and the PR is mergeable.
- CNC-27 completion gate: PR #54 is green at implementation commit `bab0eb56e4` (Linux 3m17s, Windows 3m59s); task complete at 15/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/54
- CNC-28 literal acceptance: every `ctnk` chemical tank intrinsically uses the same cloak timing and detection model as `stnk`: it cloaks after the configured initial/recloak delay without a crate or stealth generator, attack/damage events reveal it according to the shared `UncloakOn` configuration, and enemy detectors reveal it through the ordinary cloak-type mechanism. Existing health, armor, speed, weapons, prerequisites, target types, death/husk behavior, AI weights, and squad ownership remain unchanged.
- CNC-28 forbidden outcomes: adding engine or AI behavior for a config-only change; duplicating active Cloak traits; leaving intrinsic cloak dependent on `StealthGen` or `cloak-crate-collected`; becoming invisible to valid detectors; remaining cloaked while firing; altering stealth-tank behavior; changing chemical damage/balance; changing unrelated vehicles through a shared default; or introducing nondeterministic/save-replay state.
- CNC-28 contention inventory: `CTNK` inheritance from `^Tank`, `^Vehicle`, `^Cloakable`, and `^AcceptsCloakCrate`; inherited `Cloak.RequiresCondition`; stealth-generator receiver and external crate condition; shared initial/recloak delay and `UncloakOn` events; detector `CloakTypes`; targetability, auto-target/attack traits, critical-damage pause, repair, death/husk restore actor, map overrides, AI production/squad lists, and CNC-27's intentionally `stnk`-only specialist module.
- CNC-28 initial design: mirror the existing actor-local `STNK` cloak override at the `CTNK` actor boundary, replacing its crate-only inheritance with intrinsic `^Cloakable`, removing the inherited cloak requirement and stealth-generator receiver, and changing nothing else. Validate the resolved rules, then use live fixtures for initial cloak, firing/recloak, valid detector reveal/attack, damage, husk restoration, and full-match regression.
- Session state: autonomous work is active on CNC-28. Windows presentation mode was requested with `PresentationSettings.exe /start` as the reversible session keep-awake mechanism; run `PresentationSettings.exe /stop` when this autonomous run pauses or ends.
