# Autonomous CNC State

- Task sheet: `AUTONOMOUS-CNC-TASKS.md`
- Deferred work: `DEFERRED_WORK.md`
- Completed tasks: `CNC-19` (11 cycles, PR #38 green); `CNC-20` (12 cycles, PR #39 green); `CNC-21` (12 cycles, PR #40 green); `CNC-22` (8 cycles, PR #41 green); `CNC-23` (11 cycles, PR #42 green); `CNC-24` (14 cycles, PR #43 green); `CNC-24.5` (20 cycles, PR #45 green); `CNC-24.6` (14 cycles, PR #46 green); `CNC-25` (22 resumed cycles plus retained 28-cycle history, PR #44 green); `CNC-26` (12 cycles, PR #47 green); `CNC-26A` (11 cycles, PR #48 green); `CNC-66` (4 cycles, PR #51 green); `CNC-26B` (8 cycles, PR #53 green); `CNC-27` (15 cycles, PR #54 green); `CNC-28` (10 cycles, PR #55 green); `CNC-29` (13 cycles, PR #56 green); `CNC-30` (19 cycles, PR #57 green); `CNC-31` (30 cycles, PR #58 green); `CNC-32` (15 cycles, PR #59 green)
- Active task: `CNC-24.7` parallel headless MAX simulation orchestration on branch `agent/cnc24-7-parallel-simulations`. Local implementation and evidence pass at 24/30 cycles; task PR and green checks remain. Detailed current specification: `AUTONOMOUS-CNC-REPORTS/CNC-24.7.md`.
- Integration checkpoints: merged PR #49 contains cumulative work through CNC-26A; merged PR #62 contains stable cumulative work through CNC-33A; merged PR #63 disables release AI debug-log switches on `bleed`.
- Current cumulative base: `origin/bleed` after PR #63; planned task PR branch is `agent/cnc24-7-parallel-simulations`.
- Publication: one task branch and pull request; never push directly to `bleed`
- CNC-33A result: uncapped refineries; shared 90 direct/free harvester target; literal-zero critical recovery only; no throughput production pause; 10K/swapped/save-load and SkyNet gates passed. Final locked-faction VIKI regression failed, so publication is capped as `first iteration` at 33/33.
- CNC-33A environment: PR #60 is authoritative because no laptop `.worktree`/raw artifacts transferred. Linux full-engine evidence uses explicit headless MAX with llvmpipe/Xvfb and repository-declared freeware content.
- Portable skill state: repository-owned `.agents/skills/autonomous-cnc-coding/` and `.agents/skills/coding-workflow/` both contain complete `SKILL.md` instructions plus `agents/openai.yaml` discovery metadata, and `AGENTS.md` routes future Codex sessions to them. The coding-workflow `SKILL.md` is byte-identical to the installed skill at the time of publication; future machines do not need this PC's Codex home directory.
- CNC-33 evidence through cycle 10: the pure policy, strict solution builds, all 341 unit tests, and exhaustive CNC YAML/map validation pass. A natural two-bot Empire Earth MAX match ended without fatal errors and produced no false sustained congestion. High-cash engine cycles exercised cash scaling and real unload congestion, exposing and driving fixes for ordinary adaptive MCV contention, a Brutalis resonator/refinery classification error, refinery starvation by discretionary unit spending, and smart MCV requests blocked behind a busy queue. Detailed cycles 8 and 10 then isolated a one-tick opening-MCV lifecycle race: an accepted request could leave the production queue before the actor/build statistic appeared, causing duplicate opening requests. Cycle 10 is preserved as the second failed reproduction; a saved outstanding-request state with bounded canceled-request recovery is now under validation. Evidence is under ignored `.build/cnc33/evidence/`.
- CNC-33 cycles 11-12: cycle 11 was an invalid isolated-output strict-build invocation that relocated `OpenRA.ruleset`; the corrected repository-root output build and all 341 tests pass. Cycle 12 proved the lifecycle correction in the engine: exactly one opening MCV was requested, smart expansion remained serialized, and three capacity increases were observed with no duplicate in-flight request. It also reproduced five incorrect Brutalis resonator selections during unload congestion because the queue selector still read the broad legacy refinery list; the selector now consumes the manager's unloading-only refinery types. Cycle 12 logs are preserved under `.build/cnc33/evidence/cycle12-highcash-serialized/`.
- CNC-33 cycles 13-18: cycle 13 proved congestion selects only real unloading refineries (eight `proc`, zero resonators). Cycle 14's first matched differential exposed external unit requests being discarded while compatible queues were busy; the feature-disabled control consequently dominated. After preserving waiting requests and allowing independent free-queue progress, cycle 15 fulfilled both smart expansion requests but exposed excessive refinery construction. Cycle 16 swapped starting positions and gave the smart AI a material army/assets/capacity advantage, though refinery overbuilding remained. Cycle 17 ran naturally to decisive smart-AI victory but built 28 refineries because the congestion queue selector bypassed the manager's one-at-a-time reservation. Cycle 18 restored that reservation, changed congestion evidence to require every live unloading refinery to be occupied before counting overflow waiters, and passed a zero-warning build plus all 342 tests. Cycle 17 raw logs are preserved under `.build/cnc33/evidence/cycle17-ab-bounded-refinery/`; the corrected behavior is awaiting engine revalidation.
- CNC-33 cycles 19-20: cycle 19 proved one congestion reservation completed and released pressure, but exposed that the legacy minimum-refinery path had accidentally been gated by the enabled-only smart reservation, unfairly preventing the feature-disabled control from normal scaling; the invalid match was stopped and the legacy path restored. Cycle 20 was a fair matched full-engine comparison: both AIs built their ordinary first and second refineries, then only smart Skynet reacted to sustained all-refineries-occupied overflow. Exactly three congestion reservations produced three completed relief refineries with no expiry or duplicate decision; waiters repeatedly returned to zero. At the bounded 32-minute stop, smart Skynet led 132,440 to 20,500 army value, 303,190 to 22,000 assets, and 13,685 to zero current income; the control had no refinery, construction yard, or production building. This is a decisive differential acceptance pass but not a clean natural-finish adversarial pass. Evidence is under `.build/cnc33/evidence/cycle19-ab-all-refineries-occupied/` and `cycle20-ab-fair-control/`.
- CNC-33 cycles 21-22: cycle 21 reduced unrelated bot logging and reached a natural result, but deliberately varied the seed exposed an early-investment failure: smart Skynet converted 100,000 starting cash into four construction yards, fell to 8,900 assets/1,500 army, and lost to the control at 360,710 assets/145,460 army. Congestion serialization itself remained correct (one reservation, one completion, no expiry). Cycle 22 separated the lower excess-cash pressure threshold from a configurable `SmartEconomyExpansionCashPerAsset` step of 50,000: at 100,000 cash, the desired total is now three expansion assets rather than six, limiting the opening boom to one smart MCV beyond the normal two-asset opening while production capacity can still scale earlier. Zero-warning build and all 342 tests pass; engine revalidation is next. Cycle 21 evidence is under `.build/cnc33/evidence/cycle21-ab-natural-lowlog/`.
- CNC-33 added acceptance note: run well-separated matched A/B games long enough to compare both early survival/scaling and late outcome, and require consistent improvement rather than one favorable seed. Include VIKI against its previous setup because it has no silo types: unserviceable storage pressure must not stall queues, and it must still turn excessive cash into production, expansion, and army value.
- CNC-33 cycles 23-24: cycle 23 replayed the failed seed with the 50,000 expansion step. It correctly prevented overexpansion but was too conservative: after ordinary opening spend left about 91,000 cash, the smart target remained the ordinary two expansion assets and issued zero smart MCV requests. The bounded run was stopped after proving a maximum desired target of two. Cycle 24 tuned the configurable step to 40,000, which targets exactly three assets throughout the observed 80,000-119,999 range (one smart MCV beyond the normal two) and passed a zero-warning build plus all 342 tests. Cycle 23 evidence is under `.build/cnc33/evidence/cycle23-expansion-bounded-regression/`.
- CNC-33 cycles 25-28: cycle 25 showed the 40,000 step was still too conservative after ordinary opening spend. Cycle 26 changed it to 35,000 and reproduced an unsafe extra-MCV request before a combat force existed. Cycle 27 added a configurable 20% army-to-assets readiness gate; its first rerun was interrupted when the computer moved. Cycle 28 repeated the same seed and completed naturally: the smart side delayed its one extra MCV until army value was 11,520 against 44,970 assets, completed two serialized congestion-relief refineries without expiry, and won decisively at roughly 108,570 army/361,820 assets versus 8,500/12,100. The zero-warning build and all 343 tests pass. Evidence is under `.build/cnc33/evidence/cycle28-army-ready-natural-rerun/`.
- CNC-33 cycle 29: a natural native-VIKI no-silo differential proved that both sides kept every parallel production queue active (up to 20 production buildings) with zero silos and no fatal engine error. The feature-enabled side lost after never reaching sustained all-refineries-occupied congestion, while the control on the economically stronger spawn did; VIKI also lacked the HQ prerequisite required to produce an expansion MCV. This is a useful no-stall regression but an inconclusive golden comparison, not a clean adversarial pass. Evidence is under `.build/cnc33/evidence/cycle29-viki-no-silo-ab/`.
- CNC-33 cycle 30: the swapped native-VIKI differential reached a natural result with zero stderr and a valid replay. Updated VIKI kept zero-silo queues active, forced sustained congestion, made three serialized reservations under combat losses, expired two safely after refinery destruction, completed one relief refinery, and reduced measured waiters from twelve to zero. It nevertheless lost to the control (final logged assets 11,300 versus 229,670), so the golden outperformance gate and three clean post-fix adversarial passes are not proven. CNC-33 is therefore a safe `first iteration` at the 30-cycle cap. Evidence is under `.build/cnc33/evidence/cycle30-final-matrix/viki-swapped/`.
- CNC-33 follow-up diagnosis: CNC-33A is the next task. The smart refinery trigger is credibly too conservative for VIKI: it requires every refinery occupied, at least two overflow harvesters within four cells, and 750 continuous ticks before acting, then permits only one global smart reservation. More seriously, `SmartEconomyShouldReserveCashForRefinery` pauses unit production whenever pressure is active even when several thousand credits already make the refinery affordable or several facts are idle. This can suppress the combat production that extra refineries and their free ordinary/stealth harvesters should unlock. CNC-33A must calculate a bounded refinery deficit using both capacity/ratio and congestion, use multiple idle facts when justified, and reserve only an actual cash shortfall. Preserve ordinary VIKI `BuildingFractions` (`proc: 31`) and prove decisive VIKI A/B improvement without restoring refinery spam; task details are authoritative in `AUTONOMOUS-CNC-TASKS.md`.
- Cumulative rule: CNC-20 and later use the preceding task PR branch as their base
- Aged reference: insane PR may be inspected for ideas/provenance only
- CNC-25 resume state: `complete` at 22/30 resumed evidence cycles. Concurrent landed pickup and explicit post-drop assault/abort handoff passed local acceptance, three clean adversarial engine tests, final regression, 272 unit tests, strict Debug build, interface checks, CNC YAML validation, and green Linux/Windows checks at commit `8bd9305ff1`. Preserve the original 28-cycle history and regression evidence directories.
- Resumed task: `CNC-24.6` at 14/30 cycles on branch `agent/cnc24-6-headless-max`, commits `5755575ab5` and `1b7398c288`, PR #46. Local acceptance, three clean adversarial cycles, final regression, 277 unit tests, strict build, interface checks, and CNC YAML validation pass. Identical Fastest controls were demonstrably nondeterministic: control A ended at tick 2,455, while control B remained active past tick 25,431 after ten minutes, so exact cross-run replay equality is not a valid gate. Headless save/load advanced beyond tick 46,000; the five-bot high-load run reached about 500 mobile units and ended naturally at tick 16,821.
- CNC-24.6 completion gate: PR #46 is clean and both required Linux and Windows checks passed at commit `1b7398c288`; task complete at 14/30 cycles.
- CNC-26 base: cumulative branch `origin/agent/cnc24-6-headless-max` (PR #46 head). Planned branch: `agent/cnc26-tech-switching`.
- CNC-26 literal acceptance: after an enemy changes branch, Iron Reaper alone observes the branch, waits the configured approximately two game minutes, and completes the first upgrade of the counter branch (covert counters economy, recon counters covert, economy counters recon). Other AI types do not switch, premature switching is forbidden, and captured/off-branch production remains usable.
- CNC-26 contention inventory: unit and base builders, production queues, opening build-order locks, adaptive cross-queue selection, emergency-economy recovery, owned/captured prerequisite providers, upgrade downgrade/`maxupgrades` rules, actor ownership changes, and enemy observation/defeat transitions.
- CNC-26 implementation: configurable `TechnologyCounterBotModule` is gated to Iron Reaper, uses deterministic game-tick observations and counter mappings, peels incompatible ordinary branches before buying the target branch, preserves branches under `Technology Level: Extra`, pauses for emergency economy/opening-refinery recovery, and emits bounded decision/block/completion logs. Pure policy tests cover ties, delay, mappings, downgrade selection, and tier ordering.
- CNC-26 cycle evidence: cycle 1 exposed an opening-policy fight over Recon and led to removing Iron Reaper's conflicting opening unlock; cycle 2 passed Economy to Recon; cycle 3 passed a live Economy to Covert enemy transition followed by an exact 3,000-tick delayed Covert to Recon counter; cycle 4 ran to natural game over, passed Recon to Economy decision timing and emergency-economy pause/recovery, but ended before Economy III completed. Evidence is under `AUTONOMOUS-CNC-LOGS/cnc26-cycle1-*` through `cnc26-cycle4-*`.
- CNC-26 later evidence: cycle 5 cleanly completed Recon-to-Economy III; cycle 6 saved mid-observation and resumed the retained delayed decision after load; cycle 7 preserved Economy III while adding Covert III under `Technology Level: Extra`; cycle 8 reset the delay when a multi-enemy dominant branch changed from Recon to Covert and did not apply the stale counter; cycle 9 exposed and fixed faction-start initialization, then proved a Nod Iron Reaper retained and completed its owned Covert branch without an early Economy detour.
- CNC-26 final local evidence: cycle 10 completed two successive multi-enemy replans through Recon III and Economy III after adding obsolete-request cancellation; cycle 11 proved VIKI and Skynet retain ordinary adaptive upgrade behavior with zero technology-counter activity; cycle 12 was a complete graphical Fastest real match at ordinary 20,000 credits, completing Economy-to-Covert III and a live Covert-to-Recon III switch before Iron Reaper eliminated VIKI's mobile force 300-to-0 and reached the result screen. The replay is archived under `AUTONOMOUS-CNC-LOGS/cnc26-cycle12-fastest-natural-result-20260804`.
- CNC-26 local gates: strict Debug build passed with zero warnings/errors; 285 unit tests and both trait-interface checks passed. `utility.cmd cnc --check-yaml` emitted no diagnostics but exceeded a 20-minute local timeout, so it is explicitly inconclusive and GitHub Linux/Windows checks remain the authoritative whole-rules gate.
- CNC-26 completion gate: PR #47 is clean and both required checks passed at commit `1720a9b398` (Linux 3m13s, Windows 5m38s); task complete at 12/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/47
- CNC-26A base: cumulative branch `origin/agent/cnc26-tech-switching` (PR #47 head). Literal acceptance: with `Technology Level: Extra`, Iron Reaper owns and can use Economy III, Recon III, and Covert III concurrently without downgrades; with ordinary technology level it retains CNC-26 exclusive counter switching. Skynet, VIKI, Brutalis, and legacy bots remain on their authored branch behavior under both settings.
- CNC-26A cycles 1-3: an Extra-mode GDI Iron Reaper completed Covert III and Recon III while retaining its starting Economy III; the ordinary-mode regression downgraded Economy and completed only Covert III; an Extra-mode VIKI-vs-Skynet control emitted zero technology-counter lines while Skynet retained its authored Recon upgrades and Economy downgrades. No fatal/unhandled game errors occurred.
- CNC-26A cycles 4-7: a Nod-start Iron Reaper retained Covert III, completed Economy III, and requested final Recon III; a tick-9000 save captured the live Recon-to-Covert transition; two direct reloads proved in-flight Covert I ownership and ultimately completed Covert III with all three branches at tier III. The complete save/load pass is the first clean adversarial cycle.
- CNC-26A cycle 8: in an Extra-mode three-bot match, enemy dominance changed from Economy to Recon while Iron Reaper's Recon III was in flight. It retained the useful request, updated its preferred counter, and completed all branches at tick 12127 without downgrade or fatal error. This is clean adversarial cycle two.
- CNC-26A cycle 9: a 5,000-credit stress match exposed the pre-existing opening-recovery failure (four structures, zero harvesters, eventual capture). The technology module remained paused throughout and consumed no recovery queue/cash, so this is a failed stress scenario rather than a CNC-26A regression or clean adversarial pass.
- CNC-26A cycle 10: at 10,000 credits, the module stayed paused while Iron Reaper completed its refinery, eight-structure opening, five harvesters, and expansion MCV. It then survived enemy branch churn and completed all three tier-III branches at tick 13877. This is clean adversarial cycle three.
- CNC-26A cycle 11: a complete graphical Fastest Extra-mode match ran to the result screen. Iron Reaper completed all branches at tick 12377, then reduced VIKI from 100 mobile units/13 harvesters to 0/0 while finishing at 181/15. Replay and logs are archived under `AUTONOMOUS-CNC-LOGS/cnc26a-cycle11-full-graphical-fastest-result-20260804`.
- CNC-26A local gates: strict Debug build passed with zero warnings/errors and all 286 unit tests passed. The explicit-interface utility emitted no diagnostics but exceeded five minutes; like the known exhaustive-validator issue now tracked by CNC-66, it remains inconclusive pending authoritative GitHub Linux/Windows checks.
- CNC-26A publication: commit `e5883edacb` is pushed on `agent/cnc26a-all-technologies`; draft PR #48 targets `agent/cnc26-tech-switching`, is mergeable, and has green Linux/Windows checks: https://github.com/Realpra1/LibertyDawn/pull/48
- CNC-26A completion gate: PR #48 is clean and both required checks passed at commit `e5883edacb` (Linux 3m06s, Windows 4m17s); task complete at 11/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/48
- Next eligible task: CNC-66, using the post-PR-#49 base described above. CNC-66A follows it, then CNC-26B. CNC-26C is permanently pinned last: complete every other current and future task first, then ask its required user-design questions before implementation.
- CNC-66 literal acceptance: the documented Windows `utility.cmd cnc --check-yaml` path must complete the full CNC validator set with useful stage/progress diagnostics in bounded time and the same failures/results as the direct utility path. It must not enter an interactive prompt loop, silently skip maps/lints, or change LibertyDawn gameplay/content.
- CNC-66 contention/coverage inventory: Windows batch wrapper and argument forwarding, build-on-demand, utility executable startup, mod discovery/loading, lint selection, rules/sequences/maps loading, map enumeration, explicit/conditional interface checks, missing-sprite checks, process exit/timeout handling, CI shell differences, and existing launcher/debug processes.
- CNC-66 initial evidence: CNC-24.5 recorded the interactive-wrapper mistake while the direct utility executable completed full validation in 19 seconds; CNC-26 later reproduced a silent 20-minute `utility.cmd` timeout and CNC-26A reproduced a silent five-minute interface-utility timeout. No validator process is currently running, and no dedicated CNC-66 raw log exists yet.
- CNC-66 result: the Windows wrapper, not YAML linting, caused the apparent hang by ignoring supplied arguments and entering the interactive prompt. Backported official OpenRA commit `07ec2d03fbfb` behavior with child exit-code propagation. Direct validation completed in 18.132 seconds; the repaired wrapper completed in 17.765 seconds with byte-identical output. Quoted invalid-map failure propagation and interactive compatibility passed.
- CNC-66 integrated evidence: a seeded two-Skynet versus three-Brutalis headless MAX Empire Earth match ended naturally at tick 27,880 with active bots and no new fatal/Lua/unhandled error. Strict build/interface checks, 286 unit tests, and pre-report Linux/Windows CI passed. Evidence is under `AUTONOMOUS-CNC-LOGS/CNC-66/`; report is `AUTONOMOUS-CNC-REPORTS/CNC-66.md`.
- CNC-66 publication: draft PR #51 targets `agent/cnc-task-sheet-updates`: https://github.com/Realpra1/LibertyDawn/pull/51
- CNC-66A result: Empire Lars was not crashing on Windows; its apparent startup freeze came from rebuilding all 146 resonator coverage regions for thousands of resource cells during `WorldLoaded`. Event-driven invalidation reduces measured graphical startup from 30.283 seconds to 4.772 seconds while preserving powered/unpowered transitions, 7.5x configured spread acceleration, red-Tiberium instability cancellation, damage explosions, and one live timed-update chain per resource cell. A full five-AI Empire Lars match ended naturally with 99 live modifier refreshes and no scheduler mismatch or fatal error; a seven-minute Empire Earth control kept all five bots active without a new fatal error. Old replays diverge at frame 51 because the old startup accidentally scheduled the first protected resource cell twice; same-build replay playback completes normally. Report: `AUTONOMOUS-CNC-REPORTS/CNC-66A.md`.
- CNC-66A publication: draft PR #52 targets `agent/cnc66-validator`: https://github.com/Realpra1/LibertyDawn/pull/52
- CNC-66A green gate: implementation commit `592f8ff3f1` passed Linux in 3m24s and Windows in 4m42s. Remaining gate: verify startup and logs on real Mac hardware. The next autonomous task is CNC-26B.
- CNC-26B literal acceptance: while Iron Reaper owns covert technology it must actually build stealth tanks, stealth harvesters, and stealth generators and launch available nuclear support powers. The implementation should reuse the exact relevant VIKI values/behavior without importing unrelated VIKI strategy, must preserve ordinary and Extra-mode technology switching, and must not alter native VIKI/Skynet/Brutalis configuration.
- CNC-26B contention inventory: `BaseBuilderBotModule@ironreaper` building fractions/limits and placement, `UnitBuilderBotModule@ironreaper` unit whitelist/limits, shared `SupportPowerBotModule@b`, Temple prerequisites, technology-counter owned upgrades/downgrades, Extra-mode simultaneous branches, captured/off-branch production, opening/emergency-economy queue gates, and native-bot config isolation.
- CNC-26B result: Iron Reaper now uses VIKI's exact covert first-pass values for stealth generators (`2%` building ceiling), stealth harvesters (`100`, limit `50`), and stealth tanks (`15`), and excludes stealth harvesters from combat squads like VIKI. Existing shared support-power targeting remains the behavior owner; an opt-in file diagnostic records charged support-power scans and selected targets without changing target scoring.
- CNC-26B engine evidence: natural Extra-mode games built all required assets; an equal-Covert-III focused real-AI fixture issued seven Iron Reaper nuclear target orders; an ordinary-tech full match built Covert III and all required assets before Iron Reaper eliminated Brutalis 180 mobile units to 0; and a full branch-churn match downgraded Covert, completed Economy III, built no further covert assets, and ended naturally without a queue stall. Three clean adversarial cycles passed; invalid harness cycles remain recorded in `AUTONOMOUS-CNC-REPORTS/CNC-26B.md`.
- CNC-26B completion gate: strict Debug build passed with zero warnings/errors; CNC YAML completed in 20.7 seconds; both trait-interface checks passed; 300/300 unit tests passed. Draft PR #53 targets the cumulative CNC-66A branch and is mergeable; Linux passed in 3m17s and Windows in 4m42s at commit `eed40a5e8b`.
- CNC-27 literal acceptance: once an eligible AI owns at least two stealth tanks, a deterministic specialist manager reserves roughly half for at most three small stealth squads and leaves the rest available to existing ground squads. Harassment squads prioritize exposed harvesters and advanced power, may grow late for rapid fact kills, crush safely isolated infantry, and may kite an isolated shorter-range tank. A cooperative attack squad prioritizes tanks. Specialist squads must not deliberately fire inside detector, defense, or retaliation coverage unless a bounded careful-clear rule explicitly judges the local exchange favorable. Dead, captured, transported, or no-longer-eligible tanks must be released without stalling either manager.
- CNC-27 forbidden outcomes: treating stealth tanks as aircraft, importing aircraft-only ammo/repair/AA assumptions, reserving every stealth tank, issuing competing ordinary/specialist orders, cheating on hidden enemy actors beyond the AI's existing knowledge policy, repeatedly scanning the full world per tank/tick, walking through known defense or detector zones for a juicy target, firing beside ordinary retaliators, losing deterministic save/replay behavior, or changing native air-squad behavior.
- CNC-27 contention inventory: `SquadManagerBotModule` active/base/squad ownership and save data; transport reservations/missions; `STNK` cloak, hold-fire stance, crush locomotor, dual armaments, range, target types, death/husk and ownership changes; `IVisibilityModifier`/detectors; enemy weapon retaliation/range; coarse strategic targeting and threat-aware route helpers; actor-value/priority configuration; captured/off-branch units; disabled bot traits; world/local random determinism; bounded scan/update intervals; and VIKI/Iron Reaper versus legacy bot configuration.
- CNC-27 initial design: add a separate configurable stealth-squad manager plus a generic unit-reservation seam consumed by ordinary squads. Reserve zero of one tank, both of the first pair, then approximately half as numbers grow; fill two two-tank harassment groups before a cooperative attack group. Keep target scoring/threat geometry in pure helpers suitable for focused tests, issue only bounded periodic orders, and retain ordinary squad ownership for all unreserved tanks.
- CNC-27 result: a dedicated manager reserves roughly half of eligible stealth tanks into at most two harassment groups and one tank-only attack group, while generic reservations prevent contention with ordinary ground squads. Periodic live weapon/detector geometry rejects guarded routes, allows isolated shorter-range tanks to be kited, applies late fact priority only to grown groups, and includes defenses regardless of creation order. Lifecycle rebalancing releases captured/dead/ineligible actors and recreates pairs from new production.
- CNC-27 cycle evidence: cycles 3, 6, 8, and final review found and fixed respectively ground-target classification, initial reservation contention, invented zero-range detectors, and late-threat truncation. Final post-fix cycles 13-15 passed guarded late-obelisk avoidance, isolated tank/economy behavior, and capture/death/reinforcement rebalancing. Cycles 13-14 ended naturally; cycle 15 was a focused regression after its exact expected transition sequence.
- CNC-27 local gates: strict Debug build passed with zero warnings/errors; 314 unit tests, both interface checks, and the complete CNC YAML/map validator pass. Report: `AUTONOMOUS-CNC-REPORTS/CNC-27.md`. Draft PR #54 targets `agent/cnc26b-branch-config`; implementation-head Linux and Windows checks passed and the PR is mergeable.
- CNC-27 completion gate: PR #54 is green at implementation commit `bab0eb56e4` (Linux 3m17s, Windows 3m59s); task complete at 15/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/54
- CNC-28 literal acceptance: every `ctnk` chemical tank intrinsically uses the same cloak timing and detection model as `stnk`: it cloaks after the configured initial/recloak delay without a crate or stealth generator, attack/damage events reveal it according to the shared `UncloakOn` configuration, and enemy detectors reveal it through the ordinary cloak-type mechanism. Existing health, armor, speed, weapons, prerequisites, target types, death/husk behavior, AI weights, and squad ownership remain unchanged.
- CNC-28 forbidden outcomes: adding engine or AI behavior for a config-only change; duplicating active Cloak traits; leaving intrinsic cloak dependent on `StealthGen` or `cloak-crate-collected`; becoming invisible to valid detectors; remaining cloaked while firing; altering stealth-tank behavior; changing chemical damage/balance; changing unrelated vehicles through a shared default; or introducing nondeterministic/save-replay state.
- CNC-28 contention inventory: `CTNK` inheritance from `^Tank`, `^Vehicle`, `^Cloakable`, and `^AcceptsCloakCrate`; inherited `Cloak.RequiresCondition`; stealth-generator receiver and external crate condition; shared initial/recloak delay and `UncloakOn` events; detector `CloakTypes`; targetability, auto-target/attack traits, critical-damage pause, repair, death/husk restore actor, map overrides, AI production/squad lists, and CNC-27's intentionally `stnk`-only specialist module.
- CNC-28 initial design: mirror the existing actor-local `STNK` cloak override at the `CTNK` actor boundary, replacing its crate-only inheritance with intrinsic `^Cloakable`, removing the inherited cloak requirement and stealth-generator receiver, and changing nothing else. Validate the resolved rules, then use live fixtures for initial cloak, firing/recloak, valid detector reveal/attack, damage, husk restoration, and full-match regression.
- CNC-28 result: `CTNK` now mirrors the actor-local intrinsic cloak configuration already used by `STNK`. It inherits the shared cloak timing, attack/damage reveal events, detector cloak type, and critical-damage pause while removing only the inherited external-source requirement and stealth-generator receiver. Chemical balance, prerequisites, AI configuration, normal-squad ownership, repair, death, and husk actor remain unchanged.
- CNC-28 evidence: focused live fixtures proved initial cloak without a crate/generator, attack and damage reveal followed by recloak, zero hits without detection versus successful detector-enabled engagement, and a captured chemical-tank husk restoring into an intrinsically cloaked `ctnk`. External-generator destruction did not remove intrinsic cloak; critical health correctly held cloak off until healed. A unique-map golden comparison ended with seven of eight updated tanks alive and all eight old-behavior controls destroyed. A full headless MAX VIKI-versus-Brutalis match reached natural game over after exercising normal AI, chemical deaths/husks, engineer recapture, large late-game production, and no new fatal/Lua/unhandled error.
- CNC-28 local gates: strict Debug solution build passed with zero warnings/errors; 314/314 unit tests and the full CNC YAML/map validator passed. Three clean adversarial passes cover external-source/critical-health edges, the old-versus-new golden comparison, and a natural full AI match. Report: `AUTONOMOUS-CNC-REPORTS/CNC-28.md`. Draft PR #55 targets `agent/cnc27-stealth-squads`; implementation/report head `34009d7c79` passed Linux in 3m16s and Windows in 4m25s and is mergeable.
- CNC-28 completion gate: PR #55 is green at implementation/report commit `34009d7c79`; task complete at 10/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/55
- CNC-29 literal acceptance: when an eligible AI owns at least two chemical tanks, one dedicated chemical harassment group reserves approximately half while leaving the remainder available to ordinary ground squads. It considers harvesters, infantry with a bounded bonus for nearby infantry clusters, and structures whose configured armor is Tiberium-related; it never deliberately selects a tank. The group attacks infantry to exploit chemical splash instead of issuing stealth-tank crush orders, retains live weapon/detector route safety against non-infantry defenses, and releases dead, captured, transported, or otherwise ineligible actors without order contention.
- CNC-29 forbidden outcomes: duplicating the stealth-squad implementation; changing CNC-28 cloak/balance; changing existing stealth-tank group allocation or priorities; reserving all chemical tanks; creating more than one chemical specialist group; targeting any actor with the `Tank` target type even through an exact priority; treating ordinary infantry weapons as a reason to reject every intended cluster while also ignoring active detectors; targeting generic non-Tiberium structures; crushing rather than spraying infantry; competing with transports or ordinary squads; unbounded per-tick scans; or introducing nondeterministic/save-replay state.
- CNC-29 contention inventory: both specialist manager instances and `IBotUnitReservations`; ordinary squad recruitment/removal; transport reservations; `CTNK` cloak, weapon range/splash, target types, armor, crush locomotor, death/husk and ownership; `STNK` allocation/attack role; enemy weapon and detector geometry; structure `ArmorInfo.Type`; infantry target types and clusters; exact priority overrides; target incumbency/order cadence; disabled bot traits; and deterministic actor ordering.
- CNC-29 initial design: extend the existing configurable stealth-squad manager instead of copying it. Preserve the current two-harassment-plus-one-attack defaults for stealth tanks, while configuration gives chemical tanks one harassment-only group, a half-preserving reservation policy, hard tank exclusion, Tiberium-armor structure priorities, bounded infantry-cluster score multipliers, infantry-weapon tolerance without detector tolerance, and attack-not-crush orders. Add pure allocation/cluster policy tests, then use live fixtures to prove reservations, clustered-infantry preference, tank exclusion, Tiberium-building selection, threat avoidance, lifecycle release, and full-match stability.
- CNC-29 result: the existing stealth specialist manager is now configuration-driven without duplicating its implementation. Chemical tanks use one harassment-only group reserving approximately half, prioritize harvesters, clustered infantry, Tiberium-armored structures, and hostile visceroids spawned by their attacks, hard-exclude tanks, attack rather than crush infantry, and retain detector/non-infantry-defense safety. Existing stealth tanks preserve their original two-harassment-plus-one-attack allocation and priorities.
- CNC-29 evidence: focused live tests proved exact reservation transitions through capture, death, real transport boarding/unloading, and reinforcement; target selection and exclusions; detector safety; and immediate cleanup of spawned hostile visceroids. Three consecutive clean post-fix adversarial passes include a full normal-AI VIKI-versus-Brutalis MAX match to natural game over, followed by a dedicated live stealth-tank regression. Evidence and replay are under `AUTONOMOUS-CNC-LOGS/CNC-29/`; report is `AUTONOMOUS-CNC-REPORTS/CNC-29.md`.
- CNC-29 local gates: strict Debug build passed with zero warnings/errors; 322/322 tests and the full CNC YAML/map validator passed. Draft PR #56 targets `agent/cnc28-stealth-chem`; implementation/report head `c3eed0f7f9` passed Linux in 3m15s and Windows in 4m35s and is mergeable.
- CNC-29 completion gate: PR #56 is green at implementation/report commit `c3eed0f7f9`; task complete at 13/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/56
- CNC-30 literal acceptance: a configurable bot manager scans roughly every ten game seconds and assigns distinct eligible collectors to live crate actors only while those exact cells are currently visible through the bot's shroud. Ground collectors must share a passable locomotor domain with the crate and use ordinary movement; eligible helicopters must use conservative independent-air threat routing and explicitly land on a zero-stopping-threat crate cell so their configured crate crush activates. When spendable cash is zero or no owned MCV exists, every otherwise suitable collector may be reserved: visible crates are assigned first and remaining units scout distinct accessible 6x6 regions that have never been seen or were seen least recently. With no suitable mobile collector and more than one owned building, a configured nonessential, sellable building that can spawn a suitable scout may be sold at a bounded cooldown. Assignments are released and replanned on target loss, collection, ownership/death/transport changes, arrival, or bounded lack of progress.
- CNC-30 forbidden outcomes: reading or pursuing crates outside current vision; assigning multiple collectors to one crate or multiple scouts to one region; sending a ground unit across an impassable domain; issuing a normal aircraft move that never lands on the crate; landing aircraft inside known anti-air stopping coverage; commandeering transport-reserved, specialist-reserved, repairing, cargo, supply, or capture-role actors; interrupting busy ordinary units outside emergency recovery; selling the last building, an unconfigured/essential building, or one unable to yield a suitable scout; scanning every map cell every tick; unbounded path searches; nondeterministic random assignment; losing save/load state; or changing crate rewards, maps, non-CNC mods, and player commands.
- CNC-30 contention inventory: `SquadManagerBotModule` ground/air membership and repair state; `TransportManagerBotModule` carrier/passenger coordinator reservations; stealth/chemical specialist `IBotUnitReservations`; supply trucks in `SpecialOrderBotModule`; capture engineers/commandos; harvesters and MCVs; `Mobile` and `Aircraft` order resolution, crush classes, force-land semantics, landing-cell blockers, and route safety; crate visibility/lifetime/disposal/actions; `Shroud` explored versus currently visible state; `DomainIndex` locomotor connectivity; coarse-cell age and unique destinations; `Sellable`/`SpawnActorsOnSell`; disabled bots, ownership/death/cargo transitions, game saves, and deterministic actor/cell ordering.
- CNC-30 initial design: add a separate `CrateCollectorBotModule` and pure `CrateExplorationPolicy` rather than mixing exploration into supply delivery. The manager implements generic unit reservations, consults every other reservation provider, and extends transport reservations to expose pending passengers through the same generic seam. Build 6x6 coarse regions once, refresh their last-visible tick only on the bounded scan, rank never-seen/oldest regions deterministically, and use `DomainIndex` for cheap accessibility. Rank one-to-one visible crate assignments by reachable distance; use the existing conservative independent-air route/threat helpers and a final `Land` order for helicopters. Persist scan timing, region ages, sale cooldown, and live assignments. Add focused policy tests and structured debug logging, then engine-test visible/hidden crates, blocked islands, safe aircraft collection, reservation/lifecycle behavior, emergency multi-scout coverage, sale recovery, save/load, and a full normal-AI match.
- CNC-30 cycle 11 focused acceptance: a full-engine Archipelago fixture with ordinary Skynet and Brutalis modules assigned one rifleman to the visible reachable ground crate and one Orca to a visible island crate using `safe-land`; both crates were visibly consumed by 20 seconds. A second visible island crate remained uncollected because the only approach had live MSAM stopping threat 1, and a crate outside current shroud stayed untouched. Earlier cycles exposed and fixed initial scanning before shroud initialization; the module now provisionally reserves eligible collectors for one tick and performs the first scan after shroud state is current. Evidence is under `AUTONOMOUS-CNC-LOGS/CNC-30/cycle11-*`; contention, save/load, emergency sale, adversarial, and natural full-match gates remain.
- CNC-30 adversarial evidence: cycle 14 sold exactly one configured nonessential power plant on connected terrain, created a rifleman, and sent it through two distinct stale regions. Cycles 15-16 saved at tick 100 with three live assignments and loaded them back; both reachable crates were collected, persisted missions released normally, and the guarded/hidden crates remained. Cycle 18 was a natural ordinary Skynet-versus-Brutalis Desert Rats match to tick 35,466: 40 visible-crate assignments, nine safe aircraft landings, four bounded emergency sales, 1,087 distinct scout assignments, and no fatal/Lua/unhandled error. Cycle 17 separately stress-tested five bots on Empire Earth through tick 38,503 with 62 crate assignments and bounded memory before the 15-minute launcher timeout; it is stress evidence, not a clean natural-match pass.
- CNC-30 contention/final regression: the Archipelago setup activated an eight-pair Brutalis Mammoth transport wave. All eight Mammoths remained transport-reserved while the crate module selected an unreserved medium tank, proving generic reservation contention. Cycle 19 repeated the literal ground pickup, safe Orca landing, guarded-crate rejection, hidden-crate preservation, transport contention, and exact one-to-one assignments after all fixes. Strict Debug solution build, all 328 unit tests, and full CNC YAML/map validation pass locally.
- CNC-30 completion gate: PR #57 is mergeable and both required checks passed at implementation/report commit `17e15f5c53` (Linux 3m19s, Windows 4m46s); task complete at 19/30 cycles. PR: https://github.com/Realpra1/LibertyDawn/pull/57
- CNC-31 literal acceptance: with ordinary AI modules enabled, an AI that owns stealth harvesters and has accessible red Tiberium accumulates a launch allowance of about 5% of its total live harvesters per game minute, reserves selected stealth harvesters, explicitly harvests real `RedTiberium`, and only after the harvester contains that resource drives it to a reachable cell in blast range of a distinct high-value enemy fact, technology/HQ structure, refinery, war factory, or airfield. The timed existing unstable-resource explosion must occur at the intended enemy-base destination and visibly damage or destroy the target; concurrent missions must not share a target actor or destination cell.
- CNC-31 forbidden outcomes: granting red cargo synthetically; changing red-Tiberium, harvester, stealth, explosion, or building balance; sacrificing ordinary harvesters; rounding 5% into an unconditional one-harvester-per-minute minimum; accumulating an unbounded launch burst while prerequisites are absent; sending an unarmed harvester toward the enemy; unloading armed cargo at a refinery; assigning multiple bombers to the same target/place; targeting friendly, neutral, dead, captured, unreachable, or irrelevant actors; allowing ordinary harvesting, squads, crate exploration, transport, or repair orders to steal a reserved bomber; permanently reserving a stalled/lost mission; scanning the whole resource map every tick; nondeterministic target/resource selection; or losing mission/rate state across save/load.
- CNC-31 contention inventory: both `HarvesterBotModule` instances and `FindAndDeliverResources`; player-issued `Harvest`, `Deliver`, `Move`, and `Stop` resolution; `ResourceClaimLayer` and dynamic red-resource cells; `ChangesHealth` unstable timer plus `Explodes@unstable`; stealth/detector and damage behavior; refinery links/unload queues; generic `IBotUnitReservations`; ordinary ground squads, crate exploration, transport rescue/heavy-drop/assault reservations, capture/supply actors, and unit production; target ownership/death/capture, occupied/blocked approach cells, locomotor domains, route-failure recovery, actor disposal timing, save/load, and deterministic actor/cell ordering.
- CNC-31 initial design: add a dedicated configurable `RedTiberiumBombBotModule` and small pure launch/target policy. Accumulate fractional launch credit from live total harvester count only while launch prerequisites exist, cap stored credit, and spend it only when a distinct viable mission is reserved. Prefer empty eligible stealth harvesters, explicit reachable red cells, and configured target priorities multiplied by economic value. Re-evaluate resource, target, approach cell, progress, ownership, and competing reservations on a bounded scan; transition from harvest to attack only after `Harvester.IsUnstable` proves real red cargo. Extend ordinary harvester management to honor generic reservations, persist missions/rate state, and emit bounded logs for candidate rejection, launch, arming, retarget, arrival, detonation/loss, release, and rate budget.
- CNC-31 cycle 28 status: the dedicated coordinator, fractional 5%-per-minute budget, replay-safe explicit unstable-harvest order, generic reservations, real-path target approaches, deterministic retargeting, state persistence, bounded logs, and six policy tests are implemented. Three clean post-scope-fix adversarial engine passes cover distinct target destruction/re-evaluation, low-count fractional pacing plus battlefield loss recovery, and mid-mission save/load without desync. A five-bot Empire Lars stress match remained responsive beyond tick 38,000 before bounded termination. Strict zero-warning Debug build, 334/334 tests, both interface checks, `git diff --check`, and exhaustive CNC YAML/map validation pass. Publication and green GitHub gates remain; report: `AUTONOMOUS-CNC-REPORTS/CNC-31.md`.
- CNC-31 completion gate: draft PR #58 targets `agent/cnc30-crates-exploration`, is mergeable, and implementation/report commit `3985863016` passed Linux in 3m15s and Windows in 4m41s. Task complete at 30/30 cycles.
- Session state: CNC-33 is a green first iteration on draft PR #60. Autonomous continuation must select CNC-33A from `origin/agent/cnc33-smart-economy`; no presentation/keep-awake helper is currently active.
- CNC-32 literal acceptance: the normal solution and default build, style/interface, Lua, YAML, install, Windows/Linux/macOS packaging, and itch publishing paths compile, validate, install, or publish CNC plus its required shared engine/common/content-manager components only. Red Alert, Dune 2000, and Tiberian Sun content, assemblies, launchers, registrations, shortcuts, metadata, and validators are absent from those normal paths while CNC remains buildable, validated, launchable, and packageable.
- CNC-32 forbidden outcomes: deleting unsupported source/content; removing shared engine/common code required by CNC; silently skipping CNC maps/scripts/rules; leaving an unsupported mod in one platform package or normal solution; emitting stale unsupported launchers/registrations; breaking utility/interface checks, content installation, game launch, or deterministic ordinary CNC matches.
- CNC-32 contention inventory: `OpenRA.sln` project membership; CNC/Common/Game/Utility/Test project references; hidden `all` and `modcontent` manifests; Makefile and PowerShell default build/check/test/version/install commands; recursive Lua discovery; Linux/Windows/macOS package entrypoints; NSIS files/registry/shortcuts; itch launch entries; shared assembly-copy flags; CI invocation; autonomous game launchers and validators; stale local build artifacts.
- CNC-32 initial design: retain unsupported source trees and generic opt-in packaging helpers, but make every LibertyDawn normal entrypoint explicitly CNC-only. Remove the standalone D2K assembly from the normal solution and hidden interface manifest after dependency proof, scope script/rules checks to CNC and shared CNC scripts, package only CNC and modcontent/common assets, and audit produced artifacts plus real CNC games before publication.
- CNC-32 local result: normal solution/build/check/test/version/install/launch and Windows/Linux/macOS/itch packaging entrypoints are CNC-only; D2K is absent from solution and aggregate assemblies; development launchers reject RA/D2K/TS; and exact legacy unsupported artifacts are removed during CNC-only upgrades. Fresh and upgrade packaging payloads contained only CNC/Common/modcontent and launched real games.
- CNC-32 evidence: 15/30 cycles. Strict zero-warning Debug and Release builds, both interface checks, 334/334 tests, exhaustive CNC YAML/map validation, shell/JSON/XML/NSIS/static audits, fresh/upgrade package audits, three clean adversarial natural-completion games, and a final upgraded-payload natural match pass. One invalid packaged harness and one bounded five-bot stress run are retained as non-clean cycles. Report: `AUTONOMOUS-CNC-REPORTS/CNC-32.md`.
- CNC-32 raw evidence cleanup: at the user's request, approximately 6.5 GiB of generated autonomous/runtime logs were moved to the Windows Recycle Bin during final validation. No source, durable report, task/state, deferred-work, or other file was moved. The data remains recoverable until the bin is emptied.
- CNC-32 completion gate: draft PR #59 targets `agent/cnc31-red-tiberium-bomb-trucks`, is mergeable, and publication head `b5921e9220` passed Linux in 1m58s and Windows in 5m09s. Task complete at 15/30 cycles.
- CNC-33 literal acceptance: sustained measured refinery approach/unload congestion requests additional buildable refinery capacity; high stored-resource pressure requests a buildable silo before discretionary scaling; and sustained excessive spendable cash increases compatible combat-production buildings plus expansion MCVs until configured capacity catches up or cash pressure clears. Decisions are deterministic, bounded, configurable, and logged. Ordinary openings, low-power recovery, minimum refinery recovery, construction placement, parallel queues, unit production, MCV deployment, save/load, and native harvesting remain functional. In an otherwise identical economy-heavy real-match comparison, the enabled AI must decisively outperform a feature-disabled control in sustained throughput, congestion, useful capacity, economic/army value, and outcome.
- CNC-33 forbidden outcomes: counting distant harvesters merely travelling to a refinery as an unload queue; reacting to a one-scan traffic spike or starting cash; building refineries/silos/MCVs without a live buildable path; requesting several duplicate MCVs through competing unit builders; letting discretionary scaling outrank low power, opening, or missing-income recovery; permanently latching pressure after cash/queues clear; exceeding authored building limits; scanning all actors every tick; changing resource values, unload timing, harvester routing, storage balance, or non-CNC content; or introducing nondeterministic/save-replay state.
- CNC-33 contention inventory: `BaseBuilderBotModule` opening and minimum-refinery gates; both building/defense queue managers; `PlayerResources` cash/storage and queue spending; linked refinery assignments, delivery cells, harvester fullness and unload completion; refinery/silo buildability, power, placement and authored limits; adaptive production-building demand; external unit-production requests; MCV actor/build/deploy lifecycle; emergency economy and repair drains; save/load; bot condition switching; and bounded debug logging.
- CNC-33 initial design: add a pure `SmartEconomyPolicy` for sustained/hysteretic pressure and desired expansion capacity, plus one configurable once-per-second sampler owned by the enabled base builder. Count only nonempty harvesters linked to an owned refinery and already within a small configured radius of its delivery cell, subtracting the one active service slot; require persistent waiters before requesting another refinery. Keep storage pressure separate and prioritize a silo when near capacity. Reuse the existing adaptive combat-production building selector for excess cash, while the base builder issues at most one outstanding MCV request toward a cash-derived bounded expansion target. Persist timers/cooldowns, log state transitions and decisions, and leave harvester/refinery engine behavior unchanged.
